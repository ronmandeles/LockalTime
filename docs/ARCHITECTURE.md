# Lockal Time — Architecture

Status: planning blueprint, pre-implementation. This document is the single source of truth for system design decisions. Update it whenever a decision changes or a new service/data structure is added — do not let it drift from reality.

## 1. Product Summary

Social, location/time/group-based distraction-blocking app. Core value: shared, enforced presence without digital distraction, motivated through deliberately engaging gamification aimed at *starting sessions*, not at lingering inside the app. Native iOS + Android, React Native, Node.js business-logic API, Supabase (Postgres/Auth/Realtime).

**Product direction (owner decision, 2026-07):** Lockal Time is deliberately engaging/high-retention — the theory being that hooking a user on *this* app's focus-session concept reduces their overall phone use, so retention serves the mission rather than opposing it. This reversed the original restrained-engagement stance ("never reward randomness or comparison") that used to live in this section and in §9. The reversal was resolved via `docs/RETENTION_STRATEGY.md` (Phase 5 task 1): every major consumer-app retention pattern was rethought against the specific goal here (does it point at *starting a session*, or does it create a reason to linger in-app — the latter fights the product's whole purpose). §9 below states the resulting Keep/Avoid lists; `RETENTION_STRATEGY.md` has the full reasoning per pattern, including what was deliberately rejected and what's deferred pending infrastructure that doesn't exist yet (push notifications, a social graph).

## 2. Screens & Flow

1. Onboarding (permission priming)
2. Permission approval (OS-level: Usage Access / Overlay on Android, FamilyControls on iOS)
3. Auth (Google / Apple / email)
4. Home
5. Create Session (mode: `solo` | `dynamic_qr` | `static_qr`; duration: fixed X minutes/hours, or open-ended "until I close it")
6. Active Session (host/manager view — countdown or open-ended elapsed timer, live participant list, emergency exit)
7. QR Scan (join)
8. Session Details (pre-join confirmation; also reused for rejoin after a disconnect)
9. Emergency Exit (participant-initiated, forfeits bonuses, confirmation required — long-press/slide, not a tap)
10. Session Completion (points receipt, bonuses broken out separately)
11. History (filter: Solo / Group / All)
12. Stats (7-day chart, lifetime aggregates)
13. **Welcome Back / Session Interrupted** *(new)* — shown on relaunch after an involuntary disconnect. If the session is still open: "Rejoin Session" → Session Details screen directly (no QR re-scan, we already know the `session_id`). If the session ended while disconnected: routes to the normal Completion screen instead.

Known edge-case screens still needed but not yet designed (tracked in backlog): permission-denied fallback, QR expired/invalid/at-capacity, host-migration toast (new host only, small + calm, few seconds), account-linking dialog (OAuth email collision), offline/connectivity-lost banner. **All resolved as of Phase 6 task 7** — see that task's entry below and `components/StatusBanner.tsx`.

**Phase 6 (B2B)** adds two screens outside the original 13, both gated on `verified_host`/`admin` (reached from Home, never a competing top-level CTA): **Venue Management** (create a venue, view/regenerate its printed static-QR code) and **Venue Dashboard** (the B2B metrics screen, §10).

**Implemented Phase 2 task 2.7** (skeleton — no native blocking, sessions are "virtual"): Screens 4–8 all real navigator routes (`apps/mobile/src/navigation/types.ts`). Screen 7's QR Scan was manual token entry only in Phase 2 (no camera dependency); the actual `POST /sessions/join` call happens on Screen 8 (pre-join confirmation), not Screen 7, so a bad/expired/full code surfaces its specific error where the user can act on it. The edge-case screens listed above (QR expired/invalid/at-capacity) are *not* separate screens — they're error states rendered inline on Screen 8, keyed off the join outcome's failure code. **Implemented Phase 3 task 3.5:** Screen 7 now defaults to a real camera scan (`react-native-vision-camera` — pinned to the stable `^4.0.0` line, not the newer Nitro-Modules-based `5.x`) once permission is granted, with manual entry always one tap away; a scanned code and a typed one both still resolve on Screen 8, unchanged.

**Implemented Phase 4 tasks 8/9:** Screen 10 (`SessionCompletionScreen.tsx`) reads the already-finalized `session_participants`/`rewards_history` rows and renders the receipt, reached from either a normal session end or a successful Emergency Exit — both write the same shape, so the screen only branches on `exit_reason`. Screen 9 (`EmergencyExitScreen.tsx`) is reached via a subdued link on Active Session, shown only while the session is genuinely ongoing; confirmation is a 1500ms hold (a plain timer driving a state-toggled style, not `Animated.timing` — see backlog.md task 8 for why), and a successful exit resets navigation into Screen 10.

**Implemented Phase 5 task 7:** Screen 4 (`HomeScreen.tsx`) gains a gamification summary card above its original two CTAs — current streak, lifetime points, and a milestone progress bar (`docs/RETENTION_STRATEGY.md` §2's one adopted new mechanic). Deliberately does not gate the CTAs behind this fetch: the card renders real zeros immediately and fills in once `stats-repository.ts`'s reads resolve, preserving this screen's original "no session-lookup blocks the entry points" posture. Screens 11/12 (History/Stats) are not yet routes — their navigation entries land with each screen itself (tasks 8/9), not preemptively here, so no route is ever registered without a real screen behind it.

**Implemented Phase 5 task 8:** Screen 11 (`HistoryScreen.tsx`) — Solo/Group/All is local screen state (not a route param), re-querying `stats-repository.ts`'s keyset-paginated `fetchSessionHistory()` from page one on every filter change. A distinct empty state per filter, an error state with retry (re-fetch, not a silent failure), and a load-more guard against `onEndReached` firing repeatedly while a page is already in flight or after a short page has already proven there's nothing more. Reached from a subdued text link on Home (`home-history-cta`), not a third competing CTA (`DESIGN_GUIDELINES` §1) — Create/Scan remain the screen's one primary action.

**Implemented Phase 5 task 9:** Screen 12 (`StatsScreen.tsx`) — lifetime aggregates (`user_stats`, already proven server-side to equal `sum(rewards_history.points)`, so this screen just renders that authoritative value), current/longest streak, the achieved-milestone list, and a 7-day chart. The chart is seven plain `<View>` bars (no charting library exists in this codebase and none was added — `docs/DESIGN_GUIDELINES.md`'s deferred color palette means no library's default styling would fit anyway); day boundaries come from a new pure, injectable-clock `getLastSevenLocalDays()` (`services/local-days.ts`), device-local to match the server's `users.timezone` bucketing. Reached via a second subdued Home link (`home-stats-cta`), registered alongside `Stats` in `App.tsx` the same way `History` landed with its own task.

**Implemented Phase 4 task 12:** Screen 13 (`WelcomeBackScreen.tsx`) is a pre-navigator conditional gate (same shape as Onboarding/Permission Priming) driven by a persisted "last active session id" (`active-session-store.ts`, set on Active Session mount, cleared on Session Completion mount). **Correction to the line above:** it shows elapsed presence *time*, not points earned — an in-progress session has no authoritative points value (bonuses aren't decided until it ends), and the Money-Equivalent Logic Rule forbids showing anything else, including a provisional estimate. Rejoin is token-free: a new `rejoin_session()` Postgres function (mirroring `join_session()`'s row-locking shape, §8) authorizes on prior participation — any existing `session_presence_intervals` row for that user+session — instead of a QR token, since the 15-minute `QR_TOKEN_TTL_MINUTES` is far shorter than the offline/relaunch gaps this screen exists for. "Rejoin" resolves into the existing Session Details screen (reused, per the entry above) in a token-free mode; "already ended" resolves straight into Screen 10.

**Implemented Phase 6 task 7:** Screen 8 (`SessionDetailsScreen.tsx`) is rebuilt on the new session-preview endpoint (§3/§8): real details before joining — participant count, elapsed time, venue name — plus a late-join note once the completion bonus is provably out of reach (join mode: server-computed; rejoin mode: always, since reaching Welcome Back's rejoin already means a prior disconnect gap, and §7 forfeits the completion bonus unconditionally for any gap). Every join-failure code now has its own recovery affordance — Scan again / Back to Home / Retry — instead of one generic dead-end message. Screen 6 (`ActiveSessionScreen.tsx`) gains a dedicated offline banner for `participant_reconnecting`/`degraded_offline` (via the new shared `components/StatusBanner.tsx`) and now resets navigation into Screen 10 the instant status becomes `completed`/`force_terminated` — previously every non-host participant was stranded on a dead in-session screen when the session ended, since nothing ever navigated them away.

## 3. Tech Stack & Service Boundaries

| Layer | Responsibility | Rule |
|---|---|---|
| React Native app (bare workflow, **React Navigation**, **Zustand** for general app state, **XState** for session lifecycle) | UI, native module bridging, local blocker state | Never authoritative for points, QR validity, or session state — always defers to server on reconnect |
| Node.js API (**Express**, TypeScript) | Session lifecycle, QR signing/validation, points/bonus calculation, host migration worker, device attestation checks | The **only** place money-equivalent logic (points, bonuses, QR tokens) is computed or minted. Client claims of "I completed X" are never trusted. Express chosen over NestJS: our API surface is small and well-bounded (~4-5 modules), and this is a solo/small-team build where NestJS's enforced-structure benefit (paying off with many contributors) doesn't outweigh its added ceremony per atomic task. **Auth boundary** (Phase 2 task 2.0, `.claude/skills/api-design/SKILL.md`): the mobile app's Supabase access token is sent as `Authorization: Bearer <token>`; the server verifies its signature against Supabase's JWKS (`jose`'s `createRemoteJWKSet`, ES256 — the local stack's and current Supabase default; the public key is fetched once and cached, so this stays a no-round-trip check after the first request) and never trusts a client-supplied user id. **Validation:** zod schemas at each route boundary. **Errors:** every failure renders through one JSON envelope, `{ error: { code, message } }` — `code` is the real contract the mobile app switches on; `message` is diagnostic-only. |
| Supabase (Postgres) | Source of truth for all persisted state | RLS-protected; direct client reads allowed for read-only aggregates (home summary, history, stats), never for writes that affect points |
| Supabase Realtime | Live sync between clients | See §5 — Presence/Broadcast are UI-hints only; Postgres Changes (CDC) is the only trusted channel for state that affects points |

## 4. Native Blocking Modules

### Blocking policy scope

**Revised in Phase 9 (2026-08-07).** This section previously said a session blocks a *fixed set of default categories* — "not a per-session or per-user app picker", with "no new `sessions` schema needed". Both are now false. The full design and its reasoning live in [docs/BLOCKLIST_SELECTION_PLAN.md](BLOCKLIST_SELECTION_PLAN.md); the short version:

The **host chooses per session**, on the Create Session screen, and the choice is stored on the session row (`sessions.blocked_categories` / `blocked_packages`). They name two kinds of thing, both plain strings so they can travel between phones:

| Kind | Example | Meaning |
|---|---|---|
| Category | `social` | every app on *that* device declaring itself social |
| App | `com.instagram.android` | that specific app, on devices that have it |

Each member's device resolves those names against its **own** installed apps. Host picks "Social" and a member with Instagram and WhatsApp loses both, while a member with neither loses nothing.

Six categories, up from three: `social`, `games`, `entertainment`, `news`, `maps`, `productivity`. The picker pre-fills the historical three, so the new ones are opt-in.

A **category** automatically covers apps installed later; a **specific app list** does not. Neither is wrong — the UI says so, so the host chooses deliberately.

Neither platform requires us to maintain our own app-to-category database:
- **iOS:** `FamilyControls`/`ManagedSettings` restricts at the category level via `ActivityCategoryToken`, based on each app's own App Store category.
- **Android:** apps declare a category via `ApplicationInfo.category` (`CATEGORY_SOCIAL`, `CATEGORY_GAME`, etc.), queryable via `PackageManager` at runtime; we filter installed apps by category at block-time.
- **Known limitation:** Android's category field is inconsistently populated by developers (some apps are `CATEGORY_UNDEFINED`), so category-based blocking may miss a small number of mislabeled apps. Accepted limitation, not solved for MVP.
- **Known limitation (iOS, Phase 9):** an `ApplicationToken` cannot be constructed from a bundle id, read back into one, or transferred between devices, so a string arriving from our server has nothing to bind to on an iPhone. iOS members therefore re-select the session's items in Apple's own picker at join. iOS also reissues those tokens unpredictably — any design storing a token↔identity map goes stale silently, and there is no API that reports it. We accept that exposure rather than solve it (it fails in the under-blocking direction, which never buys anyone points they didn't earn); noted here alongside the Safe Mode gap below.
- **The blocklist is frozen for a session's lifetime** — not editable by the original host, and not by a host promoted through migration. That closes an exploit rather than merely simplifying: migration promotes whoever has the most minutes present (§6), which a group could arrange deliberately to hand the role to a confederate who then unblocks everything while everyone keeps earning. Kept as a *server-side policy* over a still-mutable column, deliberately, so a future add-only editing path stays cheap.

### Android
- **Mechanism: `UsageStatsManager` polling + `SYSTEM_ALERT_WINDOW` overlay + Foreground Service.** Deliberately **not** using `AccessibilityService`: Google Play tightened AccessibilityService policy with enforcement from Jan 28, 2026 — non-accessibility uses require a Play Console declaration, mandatory in-app disclosure + affirmative consent, and a stricter review process, with no exemption available to us (`isAccessibilityTool=true` is reserved for genuine disability-accessibility tools). UsageStats + Overlay avoids that review gate entirely, at the cost of ~1-2s polling lag instead of instant event detection — acceptable for this use case.
- **Boot persistence:** `BroadcastReceiver` on `BOOT_COMPLETED` checks locally-persisted session state (encrypted storage, not just Supabase — device may boot offline) and restarts the Foreground Service if a session was mid-flight at reboot.
- **Implemented Phase 3 task 3.3** (`apps/mobile/android/app/src/main/java/com/lockaltime/blocking/`): `BlockerForegroundService.kt` polls every 2s, self-detects a mid-session Usage Access/Overlay revoke (§8 item 2 — emits `permission_revoked` rather than crashing), and self-stops once the session's `endsAt` passes (a local UX safeguard only, never the server-authoritative end reason — Money-Equivalent Logic Rule). `AppBlockerModule.kt` is the JS bridge (start/stop/getStatus); events relay in-process via `RCTDeviceEventEmitter` since the service and RN instance share one process — no IPC needed. Requires `foregroundServiceType="specialUse"` + a declared subtype reason on Android 14+ (re-verify against whatever OS version is current by ship time). One correction from this doc's original wording: Android has no distinct "Entertainment" `ApplicationInfo` category, so that JS category maps to `CATEGORY_VIDEO` + `CATEGORY_AUDIO` as the closest fit — folded into the "known limitation" note above, not a separate gap. Gradle-build-verified; no physical device this phase, so on-device polling/overlay timing is manual QA pending (`docs/MANUAL_QA.md`).
- **Implemented Phase 3 task 3.4:** `BootPersistence.kt` (`androidx.security:security-crypto`, `EncryptedSharedPreferences`) saves a snapshot on every real service start and clears it on any *deliberate* stop (JS `stop()` or task 3.3's local endsAt-reached self-stop); `BootCompletedReceiver.kt` reads it on `BOOT_COMPLETED`, skipping (and clearing) a session whose `endsAt` already passed while the device was off. One accepted limitation: only listens for `BOOT_COMPLETED` (post-unlock), not `LOCKED_BOOT_COMPLETED` — `EncryptedSharedPreferences` needs credential-encrypted storage, unavailable before first unlock, so enforcement doesn't resume until the user unlocks once. Gradle-build-verified; never run on a real device this phase.
- **Implemented Phase 9 task 6 (enforcement):** the service's poll gains a `blockedPackages` set and becomes `pkg in blockedPackages || categoryOf(pkg) in blockedCategories`. A specific app **wins over its category**, and not only as an order of checks — it decides what the `shield_triggered` event says, and "Instagram is blocked" beats "social is blocked" whenever the host actually named that app. `CategoryMapping.kt` covers all six categories (`CATEGORY_NEWS`/`MAPS`/`PRODUCTIVITY` added; `CATEGORY_IMAGE` deliberately unmapped, `CATEGORY_ACCESSIBILITY` never blockable). `BootPersistence` carries the packages and the overlay copy, because a reboot restarts the service without JS ever running — omitting the packages would resume mid-session with a partial blocklist, silently under-blocking exactly the apps the host singled out. **The overlay copy now comes from JS.** It was one generic Android string resource with no i18n path at all (a bare Kotlin `TextView`, outside i18next, with no `values-iw` counterpart); the already-translated text is handed across the bridge at `start()` and the service substitutes the blocked app's own label from `PackageManager` — one translation source of truth, and the overlay finally names what it blocked.
- **Implemented Phase 9 task 4:** `InstalledAppsModule.kt` enumerates the host's launchable apps (LAUNCHER-intent filtered, so the hundreds of headless system services a raw `getInstalledApplications()` returns never reach the picker) for the Create Session app picker. Icons are a **separate, windowed** call — an earlier design returned them inline with the list, which for ~200 apps is 1.5–3 MB of base64 in one synchronous bridge payload. The default dialer and SMS app are excluded via `TelecomManager`/`Telephony.Sms`, which is the only device-accurate way to name them; JS and the server each keep a static denylist as well, since a static list is guesswork about precisely the app someone might need in an emergency. Needs `QUERY_ALL_PACKAGES` (restricted — a Play Console declaration that can be refused, `docs/DEPLOYMENT.md`); a refusal costs niche apps and no UI, because the JS seam falls back to the bundled catalog on an empty enumeration.
- **Known unfixable gap:** Android Safe Mode disables all 3rd-party apps including ours. No technical fix exists; accepted as a documented limitation, mitigated only by server-side heartbeat-gap reconciliation after the fact (see §8 threat model).

### iOS
- **Mechanism:** `FamilyControls` (authorization, requested at Screen 2) + `ManagedSettings` (Shield applied at session start) + `DeviceActivityMonitor` extension (enforces for the session's planned duration).
- **Critical constraint:** the `DeviceActivityMonitor` extension runs as a **separate OS process**, independent of the main app's lifecycle — it cannot call back into React Native directly. Communication goes through a **shared App Group container** (shared UserDefaults/file) or **Darwin notifications**, observed by the main app when foregrounded. The extension itself — not the JS layer — is responsible for clearing the shield at session end; JS only reconciles what already happened.
- **Entitlement status:** Family Controls / Screen Time API entitlement application in progress with Apple (real lead time, no guaranteed approval). Development proceeds in parallel as if approved; the native module is built and tested via TestFlight/internal builds, with the understanding that App Store submission is blocked until entitlement approval lands.
- **Implemented Phase 3 task 3.6** (`apps/mobile/ios/LockalTime/Blocking/`): `BlockingPermissionsModule.swift` requests FamilyControls authorization, then — a design nuance discovered only while implementing this, not previously scoped — presents `FamilyActivityPicker` (`ActivityPickerHostView.swift`, SwiftUI) so the user picks which categories to block. This is required, not optional: Screen Time API `ActivityCategoryToken`s are opaque and privacy-preserving by design — the app can never construct a token for "Social Networking" out of thin air, only capture whichever categories the user selects via Apple's own picker UI, once, and reapply that same saved selection on every session start (`SharedAppGroup.swift`, `Codable`-persisted). `AppBlockerModule.swift` applies the shield immediately via `ManagedSettingsStore` and schedules `DeviceActivityCenter` monitoring so the `DeviceActivityMonitorExtension` (separate process, `apps/mobile/ios/LockalTimeBlockerExtension/`) guarantees shield cleanup at session end even if the main app is suspended. **Not compiled or run anywhere in this repo** (no Mac) — written to match these frameworks' documented APIs as precisely as possible, but genuinely unverified; the JS-side wiring (`blocking-permissions.ts`/`app-blocker.ts`) IS tested (mocked native modules), since that part doesn't depend on Swift actually compiling. The extension needs a real Xcode target that isn't wired into `project.pbxproj` in this change (deliberate — see `docs/MANUAL_QA.md`'s "iOS extension target setup" for the full manual steps once a Mac is available).

### Bridge pattern (both platforms)
Native Module exposes `start(sessionConfig) / stop() / getStatus()`, plus a JS `EventEmitter` for native-observed events (`shield_triggered`, `service_killed`, `battery_critical`, `permission_revoked`). A `useAppBlocker` hook subscribes and reconciles against the Supabase session record — e.g., if native reports the service was killed and the app relaunches mid-session, the hook surfaces Screen 13 (Welcome Back) rather than leaving a zombie session.

### Offline mode
The 30-minute offline enforcement window (PRD requirement) is owned by the **native layer**, not JS — JS/React Native can be suspended by the OS while the native foreground service/extension keeps running. The native side self-enforces the cutoff and emits an event; JS only surfaces the resulting state when it wakes.

**Implemented Phase 4 task 11:** Android's `BlockerForegroundService.kt` checks `ConnectivityManager` every poll cycle (2s — precise enough for a 30-minute threshold without a separate `NetworkCallback` registration) and self-stops (lifting the block, clearing boot-persistence, emitting `offline_cutoff_reached`) once 30 continuous minutes pass with no internet-capable active network — the same "local safeguard, never the server-authoritative reason" posture as its existing `endsAt` self-stop. Gradle-build-verified (`./gradlew assembleDebug`), never run on a device this phase. iOS's `AppBlockerModule.swift` uses `NWPathMonitor` + a repeating `Timer` for the same 30-minute check — but with a genuine, documented platform difference from Android: this only runs while the module's process is alive, since iOS has no equivalent to a foreground service available to this feature (DeviceActivityMonitorExtension's callbacks are schedule-start/end/threshold events, not a continuous polling hook). A fully-suspended iOS app does not currently enforce this cutoff — an accepted limitation for now (same category as this section's Android Safe Mode gap), not something papered over. Never compiled (no Mac). Separately, client-observed realtime *socket* health (distinct from this native cutoff) now drives the lifecycle machine's `CONNECTION_LOST`/`RECONNECTED` events via `session-channel.ts`'s `channel.subscribe()` status callback (`SUBSCRIBED` → connected; `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED` → disconnected) — no new native dependency needed for this half, since it only needed the realtime channel's own connection-status callback that was already available but unused.

## 5. Realtime Architecture

Per-session channel `session:{session_id}`, using three Supabase Realtime primitives together:

1. **Presence** — each client tracks itself (`user_id`, `is_host`, `joined_at`), heartbeat ~15s. This is what host-liveness detection rides on; no persisted heartbeat table needed.
2. **Broadcast** — ephemeral events with no persistence need: timer ticks, `host_migrated`, `session_ended`, participant join/leave UI pulses. **UI-hint only — never trusted for anything that affects points.**
3. **Postgres Changes (CDC)** — durable state (`session_participants`, `session_presence_intervals` inserts/updates), so a reconnecting client gets consistent state via an initial REST fetch + resumed CDC stream rather than trusting only in-memory Broadcast events it may have missed.

**Implemented Phase 2 task 2.5:** `apps/mobile/src/services/session-channel.ts`. One correction found via a real integration test against the local stack rather than assumed: Postgres Changes enforces RLS on the *subscribing* connection, same as any REST read — an unauthenticated (anon-role) client silently receives no CDC events at all, even for a table it could otherwise query once signed in. Every real subscriber must carry an authenticated session.

## 6. Session Lifecycle & Host Authority

Sessions are **not** linear (`created → active → completed`) — model explicitly as a state graph with `host_disconnected`, `participant_reconnecting`, `degraded_offline`, `force_terminated` as first-class states (recommend XState or equivalent, not ad-hoc booleans). **Implemented Phase 2 task 2.6:** `apps/mobile/src/machines/session-lifecycle-machine.ts` (the graph) + `apps/mobile/src/hooks/use-session.ts` (the only driver — REST-hydrates, then feeds realtime events in; Broadcast events only ever drive the machine, never write `session` state directly).

- **Duration modes:** `fixed` (planned_duration_minutes set at creation) or `open_ended` ("until I close it").
- **Activation (Phase 4 decision):** a session activates immediately at creation — `status='active'` and `started_at=now()` are set in the same insert as session creation, and the host's own `session_presence_intervals` row (open, `joined_at=started_at`) is inserted at the same time. No separate "start" step exists in the screens spec (Create → straight to Active Session), so `pending` is not reachable via the current create flow — it stays modeled for a possible future explicit-start flow.
- **Host authority is real and ongoing**, not just at creation: the host can close the session at any time regardless of mode.
- **Host migration:** an in-process worker inside `apps/server` (Phase 4 decision — no separate deployment/scheduling infra exists yet, a listed known gap; revisit if/when real deployment topology is decided) subscribes to Presence for every active session. If the host's presence key is absent for **>20s** (tunable, debounced to avoid migration storms on brief drops), the server promotes whichever active participant has the **highest cumulative `minutes_present` in that session** (not earliest joiner; ties broken by earliest current-interval `joined_at`, i.e. whoever has been continuously present longest), updates `sessions.host_id`, logs it in `session_host_assignments`, and broadcasts `host_migrated`. Only the newly-promoted host gets a small, calm, few-second toast — no notification to the old host or other participants (**implemented Phase 4 task 10**: `apps/mobile/src/hooks/use-host-migration-toast.ts` watches the trusted, CDC-delivered `session.host_id` rather than the `host_migrated` Broadcast's payload, which is empty by design — Broadcast is UI-hint only, never trusted, §5). If no other participant is present to promote (including solo sessions), the session simply stays in `host_disconnected` indefinitely — no separate timeout; resolved only by the host reconnecting or by whatever end-of-lifetime mechanism already applies (the fixed-duration/24h auto-close below).
- **Stale-participant reconciliation:** the same worker closes any participant's (non-host) open presence interval, with `disconnect_reason='involuntary_disconnect'`, after their Presence key has been absent for `PARTICIPANT_PRESENCE_TIMEOUT_MINUTES` (30 min — matching the native offline-cutoff grace period, §4, so a participant isn't penalized for the same offline window the native layer is supposed to tolerate). This is what lets session-end finalization terminate even for a participant who went offline and never came back (see the new `disconnected` exit_reason below).
- **Open-ended session cap:** server-enforced max lifetime, default **24h** (config constant, not hardcoded logic), after which the server force-closes it (`end_reason='force_terminated'`); the host must create a new session to continue.
- **Fixed-duration auto-close (Phase 4 decision):** the same generalized auto-close sweep also force-closes a `fixed`-duration session once `planned_duration_minutes` has elapsed since `started_at` — fully server-authoritative rather than relying on the host's client being open (Money-Equivalent Logic Rule); this is what `end_reason='planned_duration_reached'` actually means when nobody explicitly taps "end."
- **End reasons:** `host_ended`, `planned_duration_reached` (host-triggered or auto-close), or `force_terminated` (the 24h open-ended cap only) — all three are non-punitive (`completed`) for every participant still present. Only a participant's own `emergency_exit` (or an `involuntary_disconnect` that reconciliation later finalizes as `disconnected`) is punitive (forfeits both bonuses; base points for actual minutes present are always kept).
- **Rejoin:** unlimited, any time, no cutoff window — a participant's own decision. Each rejoin opens a new `session_presence_intervals` row (does not resurrect the old one). A gap (voluntary or involuntary) always disqualifies that participant's Completion Bonus for the session, and breaks their own personal continuity toward the Group Bonus streak.

## 7. Points & Bonus Engine

**Base:** `points = total_minutes_present × 1 pt/min` — always linear, for every exit type and duration mode. No separate proration formula; "actual vs. planned" was resolved to mean nothing more than "you get points for the minutes you were actually there."

**Group Bonus (+10%):**
- Requires a continuous stretch where concurrent participant count ≥5 for ≥30 minutes.
- The streak resets fully to zero (no partial credit banking) the instant count drops below 5.
- A participant qualifies only if *their own* presence was unbroken for the entire duration of a qualifying streak (joining mid-streak still qualifies if they stay unbroken until it hits 30 min; their own disconnect/rejoin breaks their personal eligibility even if the group streak itself survives).
- **Confirmed:** resets are count-based — the clock only resets if a departure takes the group below 5. If 6 people are present and one leaves (count stays at 5), the remaining participants' clock is unaffected.
- **Confirmed:** a joining participant only counts toward the 5+ threshold once their device has completed local blocker setup (not merely tapped "join") — closes the Sybil/bonus-farming vector described in §8 item 9.

**Completion Bonus (+10%):**
- Session's actual duration must reach ≥60 minutes (for open-ended sessions: must actually run ≥60 min before the host closes it).
- Participant must have joined at the very start of the session (practical tolerance ~60s from `started_at`).
- Zero disconnects for that participant for the entire session — any gap (voluntary or involuntary) disqualifies it.
- `exit_reason` must be `completed`.

**Stacking:** additive — both bonuses together = +20%, never compounded.

**Emergency exit:** keeps base points for actual minutes present; forfeits both Group and Completion bonus entirely, regardless of how close they were to qualifying.

**Confirmed:** base rate is 1 point per minute present.

## 8. Security & Anti-Abuse Threat Model

| # | Attack / failure mode | Mitigation | Priority |
|---|---|---|---|
| 1 | Force-stop app / kill foreground service | Boot persistence + heartbeat; missing heartbeat ⇒ violation, prorate to last-seen | MVP |
| 2 | Revoke Usage Access/Overlay permission mid-session | Native module self-polls permission state; loss triggers local violation event | MVP |
| 3 | Reboot into Android Safe Mode | Unfixable by design of the OS; accepted limitation, heartbeat-gap reconciliation only | MVP (documented limitation) |
| 4 | Uninstall + reinstall mid-session | Server-side heartbeat gap is the only signal; prorate to last confirmed heartbeat | MVP |
| 5 | Manipulate device clock/timezone | Server issues authoritative timestamps; native timer re-syncs against server time, never trusts device clock alone | MVP |
| 6 | Use a second device/profile without the app at all | No technical prevention possible; explicitly out of scope — commitment device, not a jail; covered in ToS | Accepted limitation |
| 7 | Rooted/jailbroken device hooking the blocking service | **Merged into item 8's mechanism (Phase 6 owner decision)** — no separate client-side root/jailbreak probe exists or is planned: Play Integrity's/App Attest's own device verdict is exactly the trustworthy signal this needs, and a client-side check can be lied to by the very device it's probing. One device-trust tier serves both threat rows. | Post-MVP hardening |
| 8 | Emulator used to create **or** join a session, or a rooted/jailbroken device (see item 7, merged here) | Play Integrity API (Android) / DeviceCheck-App Attest (iOS); monitor-mode rollout first, then only lowest integrity tier excluded from group bonus/streak — never blocks Solo Mode or general usage. **Monitor-mode pipeline implemented Phase 2** (`apps/server/src/modules/attestation/`): every verdict + full raw response lands in `public.device_attestations` (Node-internal only, no client read path). **Trust-tier mechanism implemented Phase 6** (`attestation/trust-tier.ts`, `session_presence_intervals`/`session_participants.device_trust_tier`, `points/group-bonus.ts`'s two exclusion guards, `apply_session_stats()`'s streak-skip branch) but **shipped with `ATTESTATION_ENFORCEMENT_ENABLED=false`** — there is still no real monitor-mode data to threshold against (every verdict recorded today reads `not_configured`, the placeholder provider), so "enforcement on" would mean acting on a placeholder, not real signal. Flipping the flag is the one remaining step once real Play Integrity/App Attest credentials exist — see `docs/MANUAL_QA.md`. | MVP |
| 9 | Sybil accounts joining just to cross the "5+" bonus threshold | Verified auth only (no anonymous accounts); rate-limit account creation per device via attestation; joiner only counts toward threshold once local blocker setup completes (confirmed, see §7) | MVP |
| 10 | Modified client / direct API calls faking "session complete" | Server never accepts a client's self-report; points finalized only from server-observed heartbeats + native-attested compliance events | MVP |
| 11 | Replayed/forged Realtime broadcast messages (e.g., spoofed `session_ended`) | Broadcast is UI-hint only; anything affecting points is re-confirmed against Postgres CDC | MVP |
| 12 | QR token screenshotted/shared beyond audience, or reused after expiry | Signed token (HMAC) + server-side expiry/capacity check + per-token join rate-limiting | MVP |
| 13 | OEM aggressive battery-optimization killing the foreground service | Onboarding prompt to disable battery optimization for the app; reliability issue, not abuse | MVP (UX) |

## 9. Gamification Philosophy

Resolved by `docs/RETENTION_STRATEGY.md` (Phase 5 task 1) — see that doc for the full pattern-by-pattern reasoning behind each line below.

**Keep:** streaks (48h rolling grace; any session with real presence keeps it alive, regardless of `exit_reason` — deliberately the most forgiving version, since a punitive streak produces rage-quits rather than habit), milestones (global, session-count based, six tiers — same forgiving counting rule as streaks so the two ladders never disagree), a **visible milestone progress bar** ("3 more sessions to X" — the one new mechanic Phase 5 adds; motivation measurably rises as a goal nears, so an after-the-fact-only announcement was leaving the effect on the table), transparent point receipts (bonuses broken out separately, never bundled opaquely).

**Reject (considered under the pivot, still rejected):** variable/randomized rewards (the strongest known compulsion mechanism, but it trains *checking*, not *starting a session* — the wrong target behavior here, plus a real regulatory/ethical loot-box-adjacent line), dishonest endowed-progress framing (crediting a new user with unearned progress), a parallel level/XP ladder (duplicates what streaks + milestones already signal — more numbers past that point is clutter, not retention), idle-animating gamification elements with no state behind them (still a `DESIGN_GUIDELINES.md` §7 motion-principles violation, not just a restraint-era holdover).

**Implemented Phase 5.5:** the streak-at-risk push — the one notification pattern the retention analysis judged as cleanly passing the "points at starting a session" test. `public.claim_streak_risk_notifications()` atomically claims any streak within `STREAK_RISK_NOTIFICATION_WINDOW_HOURS` (6) of `streak_grace_expires_at` that hasn't already been notified for that exact deadline; `apps/server/src/modules/notifications/streak-risk-notifier.ts` dispatches through a `NotificationSender` seam. Same posture as Play Integrity/App Attest in Phase 2/6: no Firebase project or Apple Push credentials exist yet, so this ships as a fully-wired, tested, **inert** pipeline — `unconfiguredNotificationSender` (server) and `pushRegistration` (mobile, mirrors `nativeSignIn`'s placeholder shape) are both stand-ins for a real delivery/registration SDK, not the claim logic or dispatch composition, which run for real today. Body copy is deliberately generic (no interpolated hour count — the job fires on a threshold crossing, not a live countdown), localized via a new `users.locale` column (the server's first-ever user-facing string) rather than defaulting to English for every recipient.

**Implemented Phase 6.5:** a friends-only leaderboard, ranked by total lifetime points — the one pattern this section originally rejected outright on comparison-anxiety grounds, deliberately reopened mid-review (owner decision, see `backlog.md`'s Phase 6.5 entry) rather than left deferred. Kept narrow on purpose: **mutual, opt-in friendship only** (never a global ranking, never a one-directional follow that lets someone watch a stranger's activity), and only two numbers are ever visible for a friend — `total_points` (the one value needed to rank, read directly from `user_stats`, never recomputed) and a coarse `hadSessionToday` boolean. Everything else (`current_streak`, raw session history) stays private; `public.users`' RLS ("own row only") is never widened — search and the leaderboard aggregate are both Node-computed (`apps/server/src/modules/friends/`), the same posture as venue metrics/session preview. Discovery is by **username** (a new column, auto-generated at signup — this app had none before), not a shareable code or email search, per the owner's explicit call.

## 10. B2B / Verified Host

- `verified_host` role, manual admin approval (MVP — no self-serve approval flow).
- Static QR tied to a named `venue` record (label only — no GPS/geolocation collected or required; this is a display/grouping label for the business's printed QR code, not a location feature).
- **MVP:** in-app business screen (gated by role) showing two headline metrics: average session duration per customer, concurrent active customers.
- **V2:** separate web dashboard with richer analytics (explicitly deferred, not in MVP scope).

**Implemented Phase 6:** the manual approval flow is exactly `docs/RUNBOOK_VERIFIED_HOST.md`'s SQL runbook — `public.users.role` has no write path outside direct SQL as the table owner (Node's grant is `select`-only, `20260729000000_grant_users_select_service_role.sql`), so there was never a decision to build an admin screen against; `apps/server/src/middleware/require-role.ts` is the only new machinery, a per-request DB read (not a JWT claim, so a manual flip takes effect immediately on the caller's next request, no re-login needed).

Static QR resolved to a **venue-scoped, non-expiring token** (owner decision) rather than a per-session token: `venues.qr_token` is minted once at venue creation and stays valid — printed once, never reprinted — until a deliberate `POST /venues/:id/qr/regenerate`. Scanning it resolves to whichever `static_qr` session is currently active at that venue (`public.join_venue_session()`, `supabase/migrations/20260729000300_static_qr_venue_token.sql`) — a genuine find-or-create-shaped read, not a fixed session id. A verified host's session capacity uses a separate, higher `VENUE_SESSION_MAX_PARTICIPANTS` constant (200 vs. the default 50) — a business's foot traffic is a different shape of "group" than a friend session.

**Phase 9 adds a second manually-granted venue flag, for the same reason and by the same mechanism.** A `static_qr` session's blocklist must be a subset of `venues.approved_blocked_categories` / `approved_blocked_packages`; the server rejects anything else with `blocklist_not_venue_approved`. Without it, nothing stops a cafe seating 200 strangers and blocking a competitor's app. Approval is out of band — the owner edits the columns in Supabase, exactly as `users.role` is flipped for Verified Host, and for the same reason: there is no write path for a business to grant itself more. A new venue defaults to the three historical categories, so it is useful immediately and only needs attention if it wants to name specific apps. This stays inside the "manual flag for now, V2 for a real application flow" position; it does not settle B2B monetization.

The MVP dashboard is `GET /venues/:id/metrics` (`apps/server/src/modules/venues/get-venue-metrics.ts`) + `VenueDashboardScreen.tsx`: `concurrentActiveCustomers` (live open-interval count on the venue's active sessions), `avgMinutesPerCustomer` and `sessionsInWindow` over a trailing `VENUE_METRICS_WINDOW_DAYS=30` — aggregates only, never an individual customer's identity. Gated by role **and** ownership (checked inside the service function itself, not just the route's role middleware — role alone would let any verified host read any venue's numbers).

## 11. Open Decisions

None remaining for Phases 0-5 — all three original items (Group Bonus reset semantics, Sybil setup-completion gate, base point rate) confirmed; §7 and §8 reflect that locked spec.

Phase 6 confirmed four more before implementation (see `backlog.md`'s Phase 6 entry for the full reasoning): static QR is a venue-scoped non-expiring token, not a per-session one; Play Integrity/App Attest enforcement ships built-but-off (`ATTESTATION_ENFORCEMENT_ENABLED=false`); root/jailbreak detection merges into that same attestation trust tier rather than a second client-side mechanism; B2B metrics are per-venue/30-day/aggregates-only. No open decisions remain from Phase 6 either.

Phase 5.5 confirmed three more (see `backlog.md`'s Phase 5.5 entry): the streak-risk push fires at a 6-hour-before-expiry threshold with no quiet-hours suppression this phase; `device_tokens` is one row per `(user, platform)`, newest wins on reinstall rather than modeling multiple devices per platform; a `users.locale` column was added (new scope surfaced during planning, not silently bundled) so the server's first localized user-facing text honors `CLAUDE.md`'s i18n rule rather than defaulting every recipient to English. No open decisions remain from Phase 5.5 either.

Phase 6.5 confirmed four more, one of them mid-review (see `backlog.md`'s Phase 6.5 entry): mutual request/accept friendship, not a one-directional follow; discovery by username (a new column this app didn't have before, the owner's explicit call over two proposed alternatives); a friends-only leaderboard ranked by total lifetime points — reversing §9's original no-leaderboard default after explicit owner pushback at the plan-approval step; activity data reuses the same forgiving "any session with presence" rule streaks already use. No open decisions remain from Phase 6.5 either.

Phase 7 confirmed eight more before task-writing (see `backlog.md`'s Phase 7 entry for the full reasoning) — this phase deliberately touches nearly every item on `CLAUDE.md`'s "known gaps" list, so each was resolved rather than assumed: production `apps/server` hosting is a PaaS (Railway/Render/Fly.io-style), not serverless, since the sweep/streak-expiry/streak-risk-notification pollers are long-running `setInterval` processes that would need real rearchitecting otherwise; iOS finally gets a real compile via a cloud macOS CI runner rather than staying deferred further; minimal crash/error monitoring (Sentry or equivalent) is added before real users; the real color palette replaces the deferred neutral-grayscale tokens in this phase; placeholder ToS/Privacy Policy content is drafted this phase for owner review; data retention is "retained for the life of the account, cascade-deleted on account deletion"; obtaining real FCM/APNs and Play Integrity/App Attest credentials is in scope (not shipping v1 permanently inert on both); a TestFlight/Play internal-testing beta ring precedes public submission. Two gaps not on the original known-gaps list were also surfaced and folded in: no account-deletion flow exists anywhere (a hard App Store/Play Store requirement for any app with account creation), and the Express API has no rate-limiting or security headers. B2B monetization / a self-serve Verified Host flow stays explicitly out of scope (§10's existing "V2, deferred" call, not reopened).

Implementing the account-deletion task surfaced a real gap the plan itself hadn't named: most FKs into `public.users`/`public.sessions` had no `ON DELETE` action at all (Postgres's default, `NO ACTION`), so deleting the account of anyone with real session/venue history simply failed with a foreign-key violation — "cascade-deleted on account deletion" wasn't actually true yet for a real user. Resolved with `20260801000000_account_deletion_cascades.sql` (`docs/DATABASE.md` has the full per-table table): sessions/participation data cascades with the deleting user (or with their session's host, if the host is who deleted their account), except `rewards_history.session_id`, which is `SET NULL` rather than cascade specifically so one participant's own earned-points receipt can't be destroyed by another user's (the host's) account deletion; `venues.owner_id` is `SET NULL` (a venue is a shared B2B asset, not personal data) rather than cascading the venue away.
