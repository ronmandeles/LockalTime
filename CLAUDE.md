# Lockal Time

Social, location/time/group-based distraction-blocking app. Native iOS + Android via React Native, Node.js API for business logic, Supabase (Postgres/Auth/Realtime) for data/auth/realtime.

**Read before doing anything else:**
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — screens, tech stack, native blocking modules, realtime design, session lifecycle/host authority, points & bonus engine, security threat model
- [docs/DATABASE.md](docs/DATABASE.md) — full schema, bonus computation algorithm, config constants
- [docs/DESIGN_GUIDELINES.md](docs/DESIGN_GUIDELINES.md) — spacing/radius/typography/motion consistency rules; read before building any screen. Color palette intentionally deferred.
- [backlog.md](backlog.md) — the phased WBS; current progress and next task
- `.claude/skills/` — `code-style`, `typescript-strictness`, `supabase-integration`, `testing-standards`, `i18n`. Each is a proper Claude Code Skill (`SKILL.md` with `name`/`description` frontmatter) invokable via the Skill tool. **Binding.** Read the relevant skill(s) before writing any code; this applies to subagents too — a subagent prompt for a coding task must name the skills to read.

## Current Status

Phase 0 (bootstrap) is mostly done — see `backlog.md` checkboxes for the authoritative state. Done: monorepo scaffold, Express+TypeScript+Jest API skeleton in `apps/server` (install/test/build green), local Supabase with `users` migration + RLS + pgTAP suite, `.claude/skills/` seeded, React Native 0.86 app in `apps/mobile` (bare, TS strict, React Navigation + Zustand + XState wired, jest/lint/typecheck green). Note: `apps/mobile` is deliberately **excluded** from npm workspaces (root `workspaces` is `["apps/server"]`) so Metro/Gradle keep their own `node_modules`. **Phase 0 is complete** — CI (GitHub Actions: server + mobile + db jobs, lint/typecheck/test/pgTAP) is green on `main`. **Phase 1 (Auth & Onboarding) is complete** — i18n/RTL foundation, email-OTP auth (unit + real integration test against the local stack, run by the CI db job), signup trigger, Screens 1–3 with error states, App flow Onboarding → Permission → Auth → Home; Google/Apple and all on-device checks are manual-QA pending per `docs/MANUAL_QA.md`. **Phase 2 (Core Session State & Realtime) is complete** — session schema/migrations, create/join Node endpoints, Play Integrity/App Attest monitor-mode pipeline, realtime channel wiring, `useSession`/lifecycle machine, Screens 4–8 wired to real data (sessions still "virtual" — no native blocking yet). **Phase 3 (Native Blocker Bridge) is complete** — real Android native module (`BlockerForegroundService` + `UsageStatsManager` polling + overlay + boot persistence, all Gradle-build-verified — Android SDK/build-tools and a JDK **are** installed on this machine now, single-ABI `arm64-v8a` dev builds work, though no physical device to install-and-run on this phase), real camera QR scanning (`react-native-vision-camera`), and a full iOS Swift implementation (FamilyControls/ManagedSettings/DeviceActivityMonitor) that is written and JS-side tested but **never compiled** (still no Mac) — see `docs/MANUAL_QA.md`'s iOS section for the Xcode wiring steps once one's available. Apple's Family Controls entitlement application is drafted (`docs/apple-family-controls-entitlement-application.md`) but not yet submitted (needs the owner's Apple Developer account). **Phase 4 (Session Lifecycle Logic) is complete** — points/bonus math as pure functions, session activation + host presence interval, `POST /sessions/:id/end` finalization, emergency-exit inline finalization, blocker-ready confirmation, the in-process session sweep worker (host migration + stale-interval reconciliation + auto-close), Screens 9/10 (Emergency Exit / Session Completion), the host-migration toast, the 30-minute offline cutoff, and Screen 13 (Welcome Back) + a token-free `rejoin_session()` flow reusing Session Details. Current work: Phase 5 (Gamification & Stats) — next task: the retention-strategy analysis (see the product-direction note below; this precedes writing any gamification mechanic into the PRD).

Decided (don't re-litigate): Express (not NestJS); React Navigation + Zustand + XState for the RN app; Maestro (not Detox) for E2E; app identifier `com.lockaltime.app` (Android applicationId + iOS bundle ID); i18n from day one — English + Hebrew, including RTL support, no hardcoded UI strings; color palette stays deferred — screens use neutral grayscale design tokens, real palette swapped in later in one pass; migrations verified against local Supabase only, the user pushes to the linked production project manually; Phase 1 auth order — email auth built and fully tested first, Google/Apple wired against placeholder config and marked "manual QA pending" until real credentials exist; **no Mac currently available** — all iOS code (Swift modules, extensions, Xcode/Pod config) is authored on this PC and verified via JS-side contract tests per `.claude/skills/testing-standards/SKILL.md`, while compiling/running/device-testing iOS is marked "manual QA pending (Mac required)" and never blocks a phase; Verified Host (Phase 6) — granted manually by flipping a DB flag in Supabase, no in-app application flow yet; staging (Phase 7) — a second free-tier Supabase project (`LockalTime-staging`), migrations applied there before prod, E2E/load tests target staging or local, never production.

Known gaps not yet designed (do not assume these are decided — ask before building around them): push notification infrastructure (no `device_tokens` table exists), analytics/observability, ToS/privacy policy content, deployment/environments beyond the staging decision above, B2B monetization & a real Verified Host application flow (minimal manual-flag approach decided for now), concrete milestone definitions, data retention policy.

**Product direction — engagement/retention is a goal (owner decision, 2026-07):** the owner wants Lockal Time to be deliberately **engaging / high-retention** ("addictive"), on the theory that hooking users on *this* app reduces their overall phone use — so retention serves the mission and is a selling point, not something to shy away from. This **reverses** the restrained anti-engagement philosophy currently written in `docs/ARCHITECTURE.md` §1 & §9 (both now flagged "under revision"). No specific mechanic is decided yet — a retention-strategy analysis (see `backlog.md`) precedes writing mechanics into the PRD. **How to apply going forward:** when building anything gamification/notification/engagement-related, design *for* retention (don't invoke the old "avoid variable rewards / no comparison" rules as binding), surface where a known addictive pattern could fit, and fold it into the plan/teach step — but still stop and ask before committing to a concrete mechanic, since the specifics aren't decided.

## Working Contract

- **Teaching-first (primary goal):** the owner is using this project to learn full-stack engineering to a level that prepares them for a Google software-engineering interview and role. Every feature/phase/task therefore runs as **plan → teach → implement → teach**: (1) *before* writing code, present a plan and explain the design — what we're building, the options considered, the trade-offs, and *why* this approach — in plain terms, and get agreement; (2) *after* implementing, walk through the implementation — the files, the functions, and the concepts they demonstrate. Prioritize transferable, interview-relevant fundamentals (data structures & algorithms, system design, concurrency/consistency, API & schema design, security/trust boundaries, testing strategy, complexity trade-offs) over framework trivia; when a task naturally exercises one of these, name it and go a level deeper. This checkpoint is mandatory and supersedes pure autonomy — do **not** silently bundle tasks past a plan/teach step (see the self-paced note below).
  - **Explain from scratch, concisely:** assume the owner has almost no prior software knowledge — define the basics (e.g. what Express is, what a `package.json` is) rather than assuming them, and never leave a term or sentence resting on unstated jargon. But the owner learns *fast*, so keep every explanation tight and high-density: short, plain-language, no padding or repetition. Aim for the minimum words that make a concept click.
- **Atomic iteration, paced by the teaching checkpoint:** work through `backlog.md` tasks one at a time in order. Unlike a fully autonomous run, each task pauses at the plan/teach checkpoint above before implementation and delivers its implementation walkthrough before the next task starts — so tasks are **not** silently bundled. Each task still closes fully before the next: TDD → implementation → full suite green (`npm test`, `npm run lint`, typecheck; `supabase test db` if the DB changed) → docs/backlog updated. Stop and ask when a task turns on a product/design decision that isn't derivable from `docs/` or existing tests. Anything touching a listed "known gap" always stops and asks; never invent a decision there.
- **TDD:** every code task starts with the `.test.ts`/`.spec.tsx` file (or pgTAP equivalent), agreed as correct, *before* any implementation code is written. "Agreed as correct" means: reviewed against `docs/` and the phase DoD, with the reasoning made explicit, before implementation proceeds.
- **Documentation-first close-out:** a task isn't done until `backlog.md` has it checked `[x]` and any `.md` file whose claims changed (e.g., `ARCHITECTURE.md` for a new service, `DATABASE.md` for a schema change) is updated in the same turn.
- **Skills system:** read the relevant skill(s) in `.claude/skills/` before starting a coding task; if a needed convention doesn't exist yet, add a new `.claude/skills/<name>/SKILL.md` (with `name`/`description` frontmatter) alongside the code, not after.
- **Commits:** only when explicitly asked — self-pacing through tasks doesn't imply auto-committing. Exception: an explicitly requested unattended/background autonomous run over `backlog.md` commits **and pushes to `origin`** after each task closes (full suite green + docs updated), giving a checkpoint to recover from if something later in the run goes wrong.
- **Branching (owner decision, 2026-07-28):** every feature and every phase gets its own branch off `dev` (e.g. `phase5-gamification-stats`), merged back into `dev` once it closes (full suite green + docs updated) — solo-dev workflow, no PR review step needed, but still a real branch + merge rather than committing straight to `dev`. `main` is left alone by this cadence; when `main` gets updated from `dev` is a separate, not-yet-decided call.

## Money-Equivalent Logic Rule

Points, bonuses, QR tokens, and anything else that affects a user's earned rewards are computed and minted **only** in the Node.js API, never trusted from a client claim, and never computed client-side even for display — always fetched from the server's authoritative value. See `docs/ARCHITECTURE.md` §3 and §8 for why.
