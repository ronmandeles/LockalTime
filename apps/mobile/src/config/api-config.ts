// The Node API's base URL. Local dev default (matches apps/server's
// PORT=3000 default, apps/server/.env.example) — a real per-environment
// value arrives with the staging setup (Phase 7 decision in CLAUDE.md), same
// deferred-config status as supabase-config.ts's local demo values.
export const API_BASE_URL = 'http://127.0.0.1:3000';
