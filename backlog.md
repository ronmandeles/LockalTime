# Lockal Time — Backlog

Tracked per our working contract: one atomic task at a time, test-first, this file updated (`[x]`) the moment a task closes, alongside any `.md` file whose claims changed.

This file is the **authoritative per-task state**. For the phase-level narrative — what shipped, what's inert, what's still open — see [docs/PROJECT_STATUS.md](docs/PROJECT_STATUS.md).

## Phase 0 — Bootstrap
Prerequisites: none.

- [x] Monorepo scaffold (`apps/mobile`, `apps/server`, `supabase/`, `docs/`, `.claude/skills/`)
- [x] React Native app init (bare workflow, not Expo managed — needed for native modules; React Navigation + Zustand + XState per ARCHITECTURE.md §3) — RN 0.86.0 in `apps/mobile`, app id `com.lockaltime.app` set on both platforms, jest/lint/typecheck green (manual QA pending: compiling/running Android & iOS — no SDK platforms or Mac on this machine)
- [x] Node.js API skeleton (Express, TypeScript, Jest+supertest) — `npm install` + `npm test` + `npm run build` all verified green in `apps/server`.
- [x] Local Supabase project + CLI (`supabase start`), initial migration for `users` — local stack healthy, `users` table + RLS migrated locally and to the linked production project (`LockalTime`), pgTAP suite (12/12) green via `supabase test db`.
- [x] CI pipeline: lint + typecheck + test, green on empty-feature repo — GitHub Actions (`.github/workflows/ci.yml`): server/mobile/db jobs, Node 24, pgTAP via pinned supabase CLI; run 29637534378 fully green
- [x] `.claude/skills/` seeded: `code-style`, `typescript-strictness`, `supabase-integration`, `testing-standards` (later: `i18n`) — each a proper `SKILL.md` with `name`/`description` frontmatter

**DoD:** `npm test` and `npm run lint` pass on an empty-feature repo; local Supabase boots; CI pipeline green.

## Phase 1 — Auth & Onboarding (Screens 1–3)
Prerequisites: Phase 0.

- [x] i18n + RTL foundation: en + he locales, RTL-safe layout conventions, no hardcoded UI strings (decided in CLAUDE.md — both languages from day one) — react-i18next + react-native-localize behind `src/i18n/`, typed locale modules with compile-time + runtime key parity, `i18next/no-literal-string` lint rule, conventions in `.claude/skills/i18n/SKILL.md` (manual QA pending: on-device en↔he switch — `forceRTL` applies on next app start; see docs/MANUAL_QA.md)
- [x] Supabase Auth wiring: email first (fully tested); Google + Apple wired against placeholder config, "manual QA pending" until real credentials (per CLAUDE.md decision) — supabase-js client + email-OTP auth service + discriminated-union auth store wired into App bootstrap, config.toml Google/Apple placeholder blocks (manual QA pending: real OAuth credentials + native SDKs, end-to-end OTP flow — see docs/MANUAL_QA.md)
- [x] `users` row auto-created via trigger on signup — `handle_new_user()` SECURITY DEFINER trigger on `auth.users` (full_name → name → email local-part → 'user' fallback, ON CONFLICT DO NOTHING), pgTAP 26/26 locally; production push pending (manual, per CLAUDE.md)
- [x] Onboarding carousel (Screen 1) — 3-page FlatList carousel per DESIGN_GUIDELINES §9 (skip/Next/Get Started, dots, token sizing), design-token module `src/theme/tokens.ts` established, first-launch AsyncStorage gating in App; placeholder en+he copy flagged for the copy pass (manual QA pending: on-device RTL swipe/paging — see docs/MANUAL_QA.md)
- [x] Permission-priming screen copy/logic (Screen 2) — including the denied-permission fallback state — priming + denied states behind the `blockingPermissions` service contract (Phase-3-swappable placeholder), open-settings recovery + fail-open proceed-anyway, App flow Onboarding → Permission → Home; placeholder copy flagged (manual QA pending: real OS dialogs when the Phase 3 native module lands — see docs/MANUAL_QA.md)
- [x] Auth error states: wrong OTP, network failure, OAuth account-linking dialog — AuthScreen (Screen 3, two-step email OTP + Google/Apple via the `nativeSignIn` placeholder seam), three error states branched on `AuthFailure.kind` only, `provider_email_conflict` mapping (GoTrue collision codes) → account-linking dialog, auth gate completes the App flow Onboarding → Permission → Auth → Home; real email-OTP integration test (`apps/mobile/integration/`, `npm run test:integration`, runs in CI db job with Mailpit + custom `{{ .Token }}` email template) (manual QA pending: real provider collision + on-device session rehydration — see docs/MANUAL_QA.md)

**DoD:** new user can sign up via all 3 providers in local/dev; RLS tested — a user can only read/write their own `users` row.

## Phase 2 — Core Session State & Realtime (Screens 4–8 skeleton)
Prerequisites: Phase 1.

- [x] **API foundation** *(discovered during Phase 2 planning — `apps/server` was still the Phase 0 `/health`-only shell)*: fail-fast zod-validated env config (`src/config/env.ts`), memoized service-role Supabase client (`src/services/supabase-admin.ts`), `require-auth` middleware (`src/middleware/require-auth.ts`, verifies Supabase access tokens against Supabase's JWKS via `jose` — cached after the first fetch, no round-trip to Supabase Auth per request; corrected from an initial HS256-shared-secret design once a real integration test proved the local stack signs tokens asymmetrically, ES256 — see `src/services/supabase-jwks.ts`), one JSON error envelope (`ApiError` + `error-handler.ts`, `{ error: { code, message } }`), `.env.example` + `dotenv` wiring for local dev. New `.claude/skills/api-design/SKILL.md`. 19/19 tests green, lint/typecheck clean.
- [x] `sessions`, `session_participants`, `session_presence_intervals`, `session_host_assignments` migrations — plus `venues` (needed for the `sessions.venue_id` FK) and `device_attestations` (new — see below). `is_session_participant()` SECURITY DEFINER helper avoids RLS recursion between the read policies. All five tables: SELECT-only grant for `authenticated`, zero write grants (writes are Node-API/service-role only); `device_attestations` has no grant at all (Node-internal). `sessions` + `session_presence_intervals` added to the `supabase_realtime` publication. 56/56 pgTAP passing (`supabase/tests/venues_test.sql`, `supabase/tests/sessions_test.sql`).
- [x] Node: create-session endpoint (QR signing, `duration_mode` handling) — `POST /sessions` (`apps/server/src/modules/sessions/`): zod validation mirrors the DB CHECKs, `qr-token.ts` mints an HMAC-SHA256 token (timing-safe verification, per-mint nonce so regeneration invalidates the old token), `host_id` always from the verified JWT (never the body). `SessionsStore` seam (`sessions-store.ts`) so the service/router tests never touch a real Supabase client. New config constants (`src/config/constants.ts`): `QR_TOKEN_TTL_MINUTES`, `SESSION_MAX_PARTICIPANTS`. 35/35 server tests green, lint/typecheck clean.
- [x] Node: join-session endpoint (signature/expiry/capacity checks) — `POST /sessions/join` + `POST /sessions/:id/leave`. Validation order: HMAC signature checked in Node first (cheap, reveals nothing about which sessions exist for a garbage token), then a new atomic `join_session()` Postgres function (`supabase/migrations/20260726225700_create_join_session_function.sql`) row-locks the session (`select ... for update`) and checks existence/status/current-token-match/expiry/capacity/idempotent-rejoin in one statement — closes the TOCTOU race where two devices joining the last slot could both read "49 present" and both insert. Leave is a single scoped `UPDATE` closing the caller's own open interval (no race to guard). Real integration test proves a truly concurrent double-join at capacity=1 admits exactly one caller. 63/63 pgTAP, 54/54 server unit tests, 3/3 server integration tests green, lint/typecheck clean. **Correction found via the integration test:** `require-auth` was rewritten from HS256-shared-secret verification to JWKS-based (ES256) — the local stack signs real tokens asymmetrically, not with the shared secret assumed in task 2.0; see `src/services/supabase-jwks.ts` and the `api-design`/`supabase-integration` skill updates. Also discovered: `service_role` needs explicit table grants too (not just `authenticated`) on this Supabase CLI version — migrations updated.
- [x] Play Integrity (Android) / App Attest (iOS) check wired into create + join, **monitor-mode only** (log verdicts, no enforcement yet) — `apps/server/src/modules/attestation/`: `AttestationProvider` port (ports-and-adapters, same seam pattern as mobile's `nativeSignIn`/`blockingPermissions`), `recordDeviceAttestation()` is fail-open at two layers (a thrown provider error and a failed DB write are both swallowed, never block create/join), `device_attestations` rows carry the full raw response for Phase 6 re-analysis. Real Play Integrity/App Attest need credentials that don't exist yet (Google Play Console service account, Apple DeviceCheck key) — writing HTTP-calling adapters against unconfirmable API shapes was skipped in favor of `unconfiguredAttestationProvider`, a placeholder returning `verdict: 'not_configured'`; the pipeline around it (DB write, RLS, create/join wiring) is real and integration-tested end to end. Tracked in `docs/MANUAL_QA.md`. 58/58 server unit tests, 4/4 integration tests green.
- [x] Realtime channel wiring: Presence + Broadcast + Postgres Changes — `apps/mobile/src/services/session-channel.ts`: one channel `session:{session_id}`, `subscribeToSessionChannel()` wires Presence sync, four named Broadcast events (`host_migrated`, `session_ended`, `participant_joined`, `participant_left` — pure pass-through, UI-hint only per ARCHITECTURE.md §5), and filtered Postgres Changes on `sessions` + `session_presence_intervals` (the only trusted channel). Real integration test (`apps/mobile/integration/session-channel.integration.test.ts`) against the local stack proves both a Broadcast round-trip between two live clients and a real Postgres Changes delivery for a service-role write — the latter required signing the subscribing client in as a real participant, since Realtime's CDC stream turned out to enforce RLS on the *subscribing* connection (an unauthenticated client silently receives nothing). CI (`ci.yml`) no longer excludes the `realtime` service in the db job. 9/9 new unit tests (mocked channel), 3/3 mobile integration tests, 183/183 mobile unit suite green, lint/typecheck clean.
- [x] `useSession` hook — `apps/mobile/src/machines/session-lifecycle-machine.ts` replaces the Phase 0 placeholder with the real graph (`idle`/`pending`/`active`/`host_disconnected`/`participant_reconnecting`/`degraded_offline`/`completed`/`force_terminated` per ARCHITECTURE.md §6), 97 table-driven tests covering every (state, event) pair including every ignored one. `apps/mobile/src/services/session-repository.ts` hydrates directly from Supabase (RLS-protected — session data isn't money-equivalent, unlike writes) rather than the Node API; `apps/mobile/src/hooks/use-session.ts` composes hydrate → subscribe → feed events into the machine → expose `{ session, openIntervals, status }`, with Broadcast handlers proven to only ever drive the machine, never mutate `session` directly. 288/288 mobile unit tests, lint/typecheck clean.
- [x] Home / Create / Scan / Details screens wired to real data (no native blocking yet — sessions are "virtual") — `apps/mobile/src/services/api-client.ts` (typed, never-throws fetch wrapper for the Node API's WRITE endpoints only — create/join/leave; reads stay on session-repository.ts's direct Supabase path, a deliberate split along the money-equivalent boundary). Five screens, all real navigator routes (`apps/mobile/src/navigation/types.ts`, wired into `App.tsx`): **Home** — two-button entry (Create/Scan), no session-lookup on mount (that needs a decision not yet made, flagged inline); **Create Session** — mode/duration form → `POST /sessions` → navigates to Active Session with the id (+ QR token for dynamic_qr); **Scan** — manual QR-token entry only this phase (`services/qr-scanner.ts`: no camera dependency added — `react-native-vision-camera` needs Pod/Gradle linking this machine can't install-and-verify, deferred to Phase 3 alongside the rest of the native-module work; the seam's `ScanResult` type is ready for a camera adapter later); **Session Details** — the actual pre-join confirmation + `POST /sessions/join` call, every `JoinOutcome` failure code (`session_not_found`/`session_not_joinable`/`qr_token_expired`/`session_at_capacity`/`invalid_qr_token`) rendering its own message; **Active Session** — `useSession`-driven status/timer/live participant list (from `openIntervals`, tested under fake timers and re-rendering on a simulated CDC update). i18n en+he for all five screens, DESIGN_GUIDELINES token-based styling (no color palette, per §11). 323/323 mobile unit tests, lint/typecheck clean.

**DoD:** two devices/simulators can create and join the same session and see each other in a live participant list; Play Integrity verdicts are logged and visible for later analysis. **Verified via integration tests against the real local stack** (create→join→leave, concurrent-join-at-capacity, RLS, realtime Broadcast + Postgres Changes between two live clients, attestation pipeline) rather than two physical devices — this machine has no Android SDK platforms and no Mac (standing constraint, `CLAUDE.md`). A literal two-device run is manual QA pending; tracked in `docs/MANUAL_QA.md`.

## Phase 3 — Native Blocker Bridge
Prerequisites: Phase 2 (needs a real session to attach to).

**Planned 2026-07-27** (see conversation; reordered from a first-draft backlog to a contract-first sequence, matching the `blockingPermissions`/`nativeSignIn` seam pattern from Phases 1–2). Locked decisions:
- Blocked-category MVP list is final: **Social Networking, Games, Entertainment** (one JS constant, mapped to each platform's native category enum, passed into `start()` — not hardcoded per platform, so the list can change without a native rebuild).
- **No physical Android device available this phase** — Android native code is written and Gradle-build-verified (compiles clean) but not runtime-verified; same "manual QA pending" posture as iOS. The literal physical-device DoD below stays open until hardware exists.
- iOS gets a full real Swift implementation this phase (not a thin stub), verified via JS contract tests only (no Mac).
- Camera QR scanning (`react-native-vision-camera`, deferred from Phase 2 — see `docs/MANUAL_QA.md`) is folded in here since native Pod/Gradle linking work is already in flight this phase.

Task order:
- [x] 3.0 `AppBlockerModule` TS contract (`SessionBlockerConfig`, `BlockerStatus`, `BlockerEvent` union: `shield_triggered`/`service_killed`/`permission_revoked`/`battery_critical`) + fake implementation — contract test first. `apps/mobile/src/services/app-blocker.ts` (interface + deterministic no-op placeholder, same pattern as `blocking-permissions.ts`) + `apps/mobile/src/config/blocked-categories.ts` (locked `social`/`games`/`entertainment` constant, single source of truth for both platforms). 7/7 new tests, 330/330 mobile suite green, lint/typecheck clean.
- [x] 3.1 `useAppBlocker` hook — starts/stops the blocker off session state; feeds `offline_cutoff_reached` into `useSession`'s new `reportOfflineTimeout()` seam, which sends `OFFLINE_TIMEOUT` to `session-lifecycle-machine.ts` (note: the cutoff-detection timer itself is Phase 4's separate "Offline 30-minute native-enforced cutoff" backlog item — this task only builds the plumbing that will consume it, so it stays currently a documented no-op until CONNECTION_LOST is also wired, a still-unscheduled task); tracks local violation state (`permission_revoked`/`service_killed`/`battery_critical`) surfaced as an inline banner on Active Session (no new screen — edge-case screens stay Phase 6). `apps/mobile/src/hooks/use-app-blocker.ts` + `use-session.ts`'s `reportOfflineTimeout`, wired into `ActiveSessionScreen.tsx` (blocking stays active through `host_disconnected`/`participant_reconnecting`/`degraded_offline`, not just `active`). 352/352 mobile suite green, lint/typecheck clean.
- [x] 3.2 Swap Phase 1's `blockingPermissions` placeholder for the real Android seam, plus a battery-optimization-exemption ask alongside the existing Usage Access/Overlay grants on Screen 2 — `apps/mobile/android/app/src/main/java/com/lockaltime/blocking/BlockingPermissionsModule.kt` (Usage Access via `AppOpsManager` + Overlay via `Settings.canDrawOverlays`, reported as one combined weakest-link status; `request()` launches whichever Settings screen is still missing since there's no combined grant flow; separate `requestBatteryOptimizationExemption()`, best-effort/non-blocking per ARCHITECTURE.md §8 item 13) + `BlockingPermissionsPackage.kt`, registered in `MainApplication.kt`; manifest gains `PACKAGE_USAGE_STATS` (protected, `tools:ignore`), `SYSTEM_ALERT_WINDOW`, `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. JS side (`blocking-permissions.ts`) branches on `Platform.OS`: Android calls the real native module (payload validated at the boundary — an unrecognized status string never becomes a false grant), iOS keeps the Phase 1 placeholder until task 3.6. `PermissionPrimingScreen.tsx` gained an `AppState` 'change' listener that re-checks `getStatus()` when the app returns to active while in the denied state — required for "open Settings, grant it, come back" to ever actually resolve, since neither Settings screen returns a result to the app directly; a real grant (either path) fires the battery-optimization ask as a fire-and-forget side effect. **Gradle-build-verified**: `./gradlew assembleDebug` succeeded end-to-end with the new Kotlin — no physical device to install-and-run on this phase, so runtime behavior stays manual QA pending (`docs/MANUAL_QA.md`), but the module compiles for real, not just JS-side speculation. 362/362 mobile suite green, lint/typecheck clean.
- [x] 3.3 Android native: Foreground Service + `UsageStatsManager` polling (2s interval) + `SYSTEM_ALERT_WINDOW` overlay (not AccessibilityService — see ARCHITECTURE.md §4) — `BlockerForegroundService.kt` polls Usage Access/Overlay grants (self-detects a mid-session revoke → `permission_revoked`), finds the current foreground app via `UsageStatsManager` (`ACTIVITY_RESUMED`/`MOVE_TO_FOREGROUND` depending on API level), and shows/hides a minimal native overlay when it's in a blocked category (`CategoryMapping.kt` — Android has no distinct "entertainment" category, so that JS category maps to `CATEGORY_VIDEO`+`CATEGORY_AUDIO` as the closest fit, same accepted-limitation posture as ARCHITECTURE.md §4's existing note); also self-stops once `endsAt` passes (a local UX safeguard only — never the server-authoritative end reason, Money-Equivalent Logic Rule) and emits `battery_critical` off `ACTION_BATTERY_LOW`. `AppBlockerModule.kt` bridges JS start/stop/getStatus; events relay in-process via `RCTDeviceEventEmitter` (`AppBlockerModule.emitEvent`, called directly by the service — no IPC needed, same process). Declared `foregroundServiceType="specialUse"` + `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` (Android 14+ requirement) — re-verify against the current OS version by ship time. JS `app-blocker.ts` now branches on `Platform.OS`: Android listens on the shared `DeviceEventEmitter` (not a `NativeEventEmitter` wrapper — the Kotlin module's `addListener`/`removeListeners` are bridge bookkeeping only) with every payload boundary-validated; iOS keeps the Phase 3.0 placeholder until task 3.6. **Gradle-build-verified**; no physical device this phase, so polling/overlay timing accuracy is manual QA pending (`docs/MANUAL_QA.md`). 374/374 mobile suite green, lint/typecheck clean.
- [x] 3.4 Android native: boot-persistence `BroadcastReceiver` on `BOOT_COMPLETED`, backed by `EncryptedSharedPreferences` (native-only storage — JS isn't running yet at boot) — `BootPersistence.kt` (`androidx.security:security-crypto`) saves a snapshot on every real service start and clears it on any *deliberate* stop (JS `stop()` or the local endsAt-reached self-stop from task 3.3 — both mean "nothing to resume"), so only an unexpected teardown leaves state behind to restart from. `BootCompletedReceiver.kt` reads it on `BOOT_COMPLETED`, skips resuming (and clears) if `endsAt` already passed while the device was off, otherwise restarts `BlockerForegroundService`. No JS surface changes — this is purely native/OS-level, so it has no unit-test seam; covered by documented manual QA only (`docs/MANUAL_QA.md`), per `.claude/skills/testing-standards/SKILL.md`'s native-modules rule. **Gradle-build-verified.**
- [x] 3.5 Camera QR scanning: wire `react-native-vision-camera` into the Scan screen (Android + iOS linking) — pinned to the stable `^4.0.0` line deliberately (the latest, `5.2.0`, turned out to be a Nitro-Modules-based rewrite needing a much heavier, riskier integration — a reasonable version call, not a default `npm install`). `apps/mobile/src/components/QrCodeCamera.tsx` is a thin wrapper (`useCameraDevice('back')` + `useCodeScanner({ codeTypes: ['qr'] })` + `<Camera>`); `ScanSessionScreen.tsx` defaults to camera mode once `useCameraPermission()` reports granted, always keeps manual entry one tap away (denied/undetermined never hard-walls the join flow — same posture as `blocking-permissions.ts`), and a scanned code goes through the same Session Details join path as a typed one. Removed the now-superseded `services/qr-scanner.ts` placeholder (its `ScanResult` seam is unused now that camera scanning is real). Android: `CAMERA` permission added, Gradle-build-verified with the new native module linked. iOS: `NSCameraUsageDescription` added to `Info.plist`; Pod install / actual iOS build is manual QA pending (no Mac). 385/385 mobile suite green, lint/typecheck clean.
- [x] 3.6 iOS native: `FamilyControls` authorization + `ManagedSettings` shield + `DeviceActivityMonitor` extension + App Group bridge, same `AppBlockerModule` contract — `apps/mobile/ios/LockalTime/Blocking/` (`BlockingPermissionsModule.swift`, `AppBlockerModule.swift`, `ActivityPickerHostView.swift`, `SharedAppGroup.swift`) + `apps/mobile/ios/LockalTimeBlockerExtension/` (`DeviceActivityMonitorExtension.swift`). **Design discovery mid-task:** Screen Time API `ActivityCategoryToken`s are opaque by design — the app can never construct "Social Networking" as a token itself, only capture whatever the user selects via Apple's own `FamilyActivityPicker` UI (once, persisted, reapplied every session) — this wasn't previously scoped and is now folded into the permission-grant flow (`BlockingPermissionsModule.request()`), not a separate screen. JS side fully wired and tested: `blocking-permissions.ts`/`app-blocker.ts` register both platforms' native modules under the same names, so `getStatus`/`start`/`stop` need no `Platform.OS` branching — only event *delivery* still differs (Android: shared `DeviceEventEmitter`; iOS: `NativeEventEmitter` wrapping the module, since iOS's module is an `RCTEventEmitter` subclass). **Per an explicit decision this run: Swift source only, no hand-edited `project.pbxproj`** — the Xcode target/capability/bridging-header wiring is a documented manual step (`docs/MANUAL_QA.md`'s "iOS native blocker bridge" section) for whenever a Mac is available; none of this Swift has been compiled or run anywhere in this repo. 396/396 mobile suite green (JS side only — Swift is unverified), lint/typecheck clean.
- [x] 3.7 Apple Family Controls entitlement application drafted (parallel track, not blocking dev) — `docs/apple-family-controls-entitlement-application.md`: app description, why Family Controls/ManagedSettings/DeviceActivity specifically are needed, privacy commitments, and a "know before you submit" note on the real framing risk (this is a self-directed focus app requesting `.individual` authorization, not a parental-control app — Apple's review is stricter for that use case, though publicly broadened to allow it). **Submission itself is yours to do** — it needs your Apple Developer account/Team ID; I can't submit on your behalf.

**DoD:** on a physical device, starting a session actually blocks a test app; killing the app or rebooting the device does not lift the block prematurely. **Not literally met** — no physical Android device and no Mac on this machine this phase (standing constraint). Android is Gradle-build-verified (compiles clean, real code, not speculative) but never installed-and-run; iOS is written but never compiled at all. Both are manual QA pending (`docs/MANUAL_QA.md`'s dedicated sections) — this is the same posture Phase 0–2 already carry for on-device checks, now extended to the phase whose entire job is native code.

## Phase 4 — Session Lifecycle Logic
Prerequisites: Phase 3.

- [x] Points/bonus math as pure functions (base rate, group bonus, completion bonus, stacking) — **test-first**, spec fully confirmed in ARCHITECTURE.md §7 — `apps/server/src/modules/points/` (`base-points.ts`, `group-bonus.ts`, `completion-bonus.ts`, `compute-rewards.ts`): `computeSessionRewards()` (whole-session, called at session end) and `computeForfeitedReward()` (single-participant base-only shortcut for inline emergency-exit finalization). Group bonus uses a sweep-line/interval-merge reconstruction of the concurrent-participant-count timeline (classic "meeting rooms II" technique) gated by a new `blocker_ready_at` per-interval timestamp (Sybil-resistance gate, §7/§8 item 9) rather than raw `joined_at` — see DATABASE.md's corrected step 5/6. Both bonuses stack additively with a single rounding pass at the end (not per-bonus) to avoid compounding drift. 35/35 new tests, 97.93% coverage on the module (skill floor: 90%), 93/93 server suite green, lint/typecheck clean.
- [x] Phase 4 schema migration (`supabase/migrations/20260728120000_phase4_lifecycle_and_rewards.sql`) — `rewards_history` created (RLS: own rows only via `user_id = auth.uid()`, not `is_session_participant()`, since it's a per-user receipt not shared session data); `sessions.end_reason` gains `force_terminated` (the 24h open-ended cap); `session_participants.exit_reason` gains `disconnected` (went offline, never reconnected before session end — forfeits both bonuses like `emergency_exit`, tracked separately); `session_presence_intervals` gains nullable `blocker_ready_at` (Sybil-resistance gate consumed by `points/group-bonus.ts`). New `PARTICIPANT_PRESENCE_TIMEOUT_MINUTES=30` constant. 74/74 pgTAP passing (`supabase/tests/phase4_lifecycle_test.sql`, 11 new).
- [x] **Session activation + host presence interval** *(discovered during Phase 4 planning — nothing previously transitioned a session from `pending` to `active`, and the host had no `session_presence_intervals` row at all, only `session_host_assignments`)*: `createSession` (`apps/server/src/modules/sessions/create-session.ts`) now sets `status='active'`/`started_at=now()` in the same insert (no separate "start" step exists anywhere in the screens spec) and opens the host's own presence interval immediately, the same as a participant's `/join` does — required for the host to earn any points at all. Consequence surfaced by the real integration test: the host now occupies one `SESSION_MAX_PARTICIPANTS`/`max_participants` slot from creation, same as any other participant (`apps/server/integration/sessions.integration.test.ts` updated accordingly). `SessionsStore` gains `insertPresenceInterval()`. 95/95 server unit tests, 4/4 integration tests green, lint/typecheck clean.
- [x] **`POST /sessions/:id/end` + reward finalization** *(discovered during Phase 4 planning — the backlog didn't name a session-end endpoint explicitly, but Screens 9/10 both need one to exist first)*: `apps/server/src/modules/sessions/end-session.ts` — host-only HTTP endpoint (`sessions.router.ts`), also callable directly with `endedBy: null` by the future auto-close sweep (task 7) so both paths share one finalization path. Closes every open interval (`disconnect_reason='session_ended'`), builds a `ParticipantSummary` per user **including already-finalized ones** (their presence must still count toward the group bonus's concurrent-count timeline for everyone else, even though their own row isn't rewritten — a real bug caught by a dedicated test before it shipped), derives each not-yet-finalized participant's `exit_reason` from their newest interval's `disconnect_reason` (`session_ended`→`completed`, else→`disconnected`), calls `computeSessionRewards()`, and writes `session_participants` + one `rewards_history` row per bonus category actually earned. `points/compute-rewards.ts` gained a `basePoints`/`groupBonusPoints`/`completionBonusPoints` breakdown (split from the single authoritative `pointsEarned` total, not re-derived) so those separate rows exist for Screen 10's "bonuses broken out separately" receipt. Uses an injectable clock (testing-standards skill) after an initial test caught the function racing real wall-clock time against fixture timestamps. `SessionsStore` gains 7 new methods. Real integration test against local Supabase (create→join→end, RLS on `rewards_history`, idempotent re-end rejected with 409). 36/36 points-module tests (98.11% coverage), 13/13 end-session tests, 114/114 server suite green, 5/5 integration tests, lint/typecheck clean.
- [x] **`POST /:id/leave` emergency-exit inline finalization** *(the server half of "Emergency exit flow end-to-end" — Screen 9 itself is still pending)*: `apps/server/src/modules/sessions/finalize-emergency-exit.ts` — after `closeOpenInterval()` closes the interval with `disconnect_reason='emergency_exit'`, calls `computeForfeitedReward()` (base points only, both bonuses forfeited unconditionally, no need to wait for session end since neither bonus depends on the rest of the session) and writes one `session_participants` row + one `base` `rewards_history` row immediately. `involuntary_disconnect` never finalizes here — the participant may still reconnect, so their fate isn't decided until the stale-interval reconciliation worker or `end-session.ts` settles it (task 7). `SessionsStore` gains `getUserPresenceIntervals()`. Real integration test extended to assert the finalized rows land correctly against local Supabase. 6/6 new tests (finalize-emergency-exit + router wiring), 121/121 server suite green, 5/5 integration tests, lint/typecheck clean.
- [x] **Emergency exit flow end-to-end (Screen 9)**: `apps/mobile/src/screens/EmergencyExitScreen.tsx`, reached via a subdued text link on Active Session (shown only while the session is genuinely ongoing — `active`/`host_disconnected`/`participant_reconnecting`/`degraded_offline`, hidden once terminal). Per DESIGN_GUIDELINES §7, the confirmation is a **hold**, not a tap — implemented as a plain `setTimeout` (1500ms) driving a state-toggled button style, deliberately *not* `Animated.timing`: a JS-driven (RAF-based) animation racing this screen's real-timer-driven confirmation logic produced overlapping `act()` scopes in tests — a genuine interaction hazard, not just a test inconvenience, so the decorative fill was dropped rather than fought. Releasing early cancels cleanly and re-arms for a later full hold. On success, calls `leaveSession(sessionId, 'emergency_exit')` (already wired server-side since task 5) and `navigation.reset`s into `SessionCompletion` (never `navigate`, so the dead session/exit screen isn't reachable via back-gesture); on failure, shows an inline error and stays put, retryable. **Discovered while testing:** fake timers fought badly with `@testing-library/react-native`'s own async auto-cleanup for a component whose state updates span a real `await` inside a timer callback — switched to real timers with real waits, and every `fireEvent`+wait pair wrapped in one `act(async () => {...})` block so React tracks the resulting updates as one coherent scope; explicit `unmount()` in `afterEach` rather than relying on the library's own cleanup timing. 10/10 new tests (7 screen + 3 Active Session link), 426/426 mobile suite green, lint/typecheck clean.
- [x] **Completion flow end-to-end (Screen 10)** *(built before Screen 9 — Emergency Exit's successful path navigates here, so the dependency runs this direction, not the backlog's listed order)*: `apps/mobile/src/screens/SessionCompletionScreen.tsx`, reachable from either a normal session end or a successful Emergency Exit — both write the same `session_participants`/`rewards_history` shape, so the screen never branches on how it got here, only on the already-computed `exit_reason` (`completed` → "Session complete"; `emergency_exit`/`disconnected` → "You left early" + a forfeited-bonuses note). Reads via two new `session-repository.ts` functions (`fetchSessionParticipant` — `maybeSingle`, since a null result right after finalization is a real expected state, not an error; `fetchRewardsHistory`), both direct RLS-protected Supabase reads of already-server-computed values (never recomputed client-side, per the Money-Equivalent Logic Rule). Bonuses always rendered as separate line items (ARCHITECTURE.md §9), only for whichever categories were actually earned. "Done" resets navigation to Home (not `navigate`) so the finished session/receipt is never reachable via back-gesture. New `EmergencyExit`/`SessionCompletion` route types added to `navigation/types.ts` (the former not yet wired into `App.tsx` — that lands with Screen 9). i18n en+he for both screens' copy (placeholder, flagged for the deferred copy pass). **Discovered while testing:** a real `useAuthStore` Zustand store combined with an async data-fetching effect produced flaky cross-test pollution in RTL (a still-settling effect from one test reacting to the next test's setup) — fixed by mocking the hook directly instead, the same seam pattern already used for `useSession`/`useAppBlocker` elsewhere in this codebase, which also more correctly isolates the screen's own logic from the store's real implementation. 9/9 new `session-repository.ts` tests, 10/10 new screen tests, 416/416 mobile suite green, lint/typecheck clean.
- [x] **Blocker-ready confirmation endpoint** *(the mechanism behind the already-locked "counts toward the 5+ threshold only once local blocker setup completes" rule, ARCHITECTURE.md §7 — needed by task 1's group-bonus math but not itself a named backlog line)*: `POST /sessions/:id/blocker-ready` (advisory, idempotent, always 200 — `SessionsStore.markBlockerReady()` only sets `blocker_ready_at` if it's still null, so a duplicate call never shifts an earlier legitimate timestamp). Mobile: `api-client.ts` gains `markBlockerReady()`; `useAppBlocker` calls it fire-and-forget once `module.start()` genuinely resolves (never on a failed start), so a real device confirming its native blocker started is what actually gates the Group Bonus threshold now, closing the loop the points module's tests already assumed. 3/3 new server tests + 1 real integration test (idempotency verified against local Postgres), 6/6 new mobile tests, 401/401 mobile suite green, lint/typecheck clean both workspaces.
- [x] **Session sweep worker** (host migration + stale-participant reconciliation + open-ended/fixed-duration auto-close, folded into one in-process poller per the Phase 4 planning decision) — `apps/server/src/modules/sessions/sweep.ts` (`runSweep()`, the testable core: auto-close first so a session past its lifetime doesn't also churn through migration, then stale reconciliation, then host migration — order matters, see inline comments) + `session-realtime-port.ts` (real Supabase Realtime Presence/Broadcast wiring, server-side counterpart to the mobile app's `session-channel.ts`) + `test-support/fake-sessions-store.ts` (a shared in-memory relational fake, since auto-close calls through to `end-session.ts` and needed realistic multi-table behavior across scenarios). Host promotion uses cumulative `minutes_present` across every interval (not just the current one — a rejoined participant keeps credit), ties broken by earliest current-interval `joined_at`; a participant reconciled away as stale in the same pass is correctly excluded from promotion candidates (a real bug caught by a dedicated test before it shipped, the same category of catch as task 4's). New `apps/server/src/server.ts` wiring runs the sweep on `SESSION_SWEEP_INTERVAL_SECONDS` (10s). **Real integration test** (`session-sweep.integration.test.ts`) proves actual host migration end-to-end against local Supabase Realtime — two live client connections, one going silent, migration firing within the real 20s debounce window — which caught a second real bug: `session_host_assignments` was missing a `service_role` `UPDATE` grant (new migration `20260728130000_grant_host_assignments_update.sql`). 17/17 new sweep unit tests (98.38% coverage), 141/141 server suite green, 7/7 integration tests (both files), lint/typecheck clean. Also fixed an unrelated pre-existing syntax error found in `server.ts` (a stray character breaking `tsc`, present in the uncommitted working tree, never previously committed).
- [x] **Host-migration toast (new host only, calm, few seconds)** — `apps/mobile/src/hooks/use-host-migration-toast.ts`. The `host_migrated` Broadcast carries no payload (Broadcast is UI-hint only, ARCHITECTURE.md §5 — never trusted, and the sweep worker's `broadcastHostMigrated()` sends nothing to trust anyway), so the toast instead watches the durable, CDC-delivered `session.host_id` already flowing through `use-session.ts` and fires only on a genuine transition *into* the current user — never on initial hydration where the current user already happens to be host (the session's own creator), which a naive "am I currently the host" check would have wrongly caught. Auto-dismisses after 4s. Wired into `ActiveSessionScreen.tsx`. 7/7 new hook tests, 2 new screen tests (26 total for the two files), 435/435 mobile suite green, lint/typecheck clean.
- [x] Open-ended session 24h auto-close job — folded into the sweep worker above (`maybeAutoClose()`), alongside the fixed-duration case `end_reason='planned_duration_reached'` already covers.
- [x] **Offline 30-minute native-enforced cutoff** — two genuinely separate halves, per ARCHITECTURE.md §4's "Offline mode" implementation note: (1) the native-enforced cutoff itself, Android real (`BlockerForegroundService.kt` checks `ConnectivityManager` every 2s poll, self-stops after 30 continuous offline minutes, Gradle-build-verified) and iOS real-but-unverified (`AppBlockerModule.swift`'s `NWPathMonitor` + `Timer`, with an honestly-documented platform gap: only runs while the module's process is alive, no Mac to compile); (2) client-observed realtime *socket* health (a different, JS-only concern) — `session-channel.ts`'s `channel.subscribe()` status callback now drives `CONNECTION_LOST`/`RECONNECTED` in `use-session.ts`, needing no new native dependency since the callback already existed unused. 7/7 new JS tests (5 session-channel + 2 use-session), 442/442 mobile suite green, lint/typecheck clean, Android Gradle build green.
- [x] **Screen 13 (Welcome Back / Session Interrupted) + rejoin flow reusing Session Details screen** *(discovered during planning: `join_session()` requires a currently-valid QR token, expiring after 15 minutes — far shorter than the offline/relaunch gaps this screen exists for, so token-based rejoin was unusable here)*: new `public.rejoin_session()` Postgres function (`supabase/migrations/20260728140000_create_rejoin_session_function.sql`) mirrors `join_session()`'s row-locking shape but authorizes on prior participation (any existing `session_presence_intervals` row for that user+session) instead of a token — returns `joined`/`already_joined`/`not_found`/`not_joinable`/`not_a_participant`/`at_capacity`. `POST /sessions/:id/rejoin` (`apps/server/src/modules/sessions/rejoin-session.ts` + router wiring) is the thin Node service delegating to it, same shape as `join-session.ts`. Mobile: `active-session-store.ts` (new, mirrors `onboarding-store.ts`'s hydrating/ready pattern) persists only a session id — never money-equivalent data — set on `ActiveSessionScreen` mount and cleared on `SessionCompletionScreen` mount; `api-client.ts` gains `rejoinSession()`. `SessionDetailsScreen.tsx` now takes a discriminated route-param union (`{ token }` join mode | `{ sessionId }` rejoin mode — `RootStackParamList` updated) and branches only on which is present, reusing the same screen/CTA per the backlog's own framing. New `WelcomeBackScreen.tsx` (Screen 13) is a pre-navigator conditional gate (same shape as Onboarding/Permission Priming — App.tsx renders it before `NavigationContainer` exists, so it resolves an outcome via `resolveWelcomeBack()` rather than navigating itself); `App.tsx` reads that resolution to pick the one subsequent `NavigationContainer` mount's `initialRouteName`/`initialParams` (`SessionDetails` for rejoin, `SessionCompletion` for "already ended", `Home` otherwise). **Correction to ARCHITECTURE.md's Screen 13 description surfaced during implementation:** it shows elapsed presence *time*, not "points earned so far" — an in-progress session has no authoritative points value (bonuses aren't decided until it ends), and the Money-Equivalent Logic Rule forbids showing anything else, including a provisional estimate; ARCHITECTURE.md §2 updated accordingly. 6/6 new pgTAP (80/80 total), 12/12 new server tests + 1 new real integration test (create→join→disconnect→rejoin, plus a stranger correctly rejected as `not_a_prior_participant`), 11/11 new `active-session-store` tests, 2/2 new `api-client` tests, 7/7 new `SessionDetailsScreen` tests, 6/6 new `WelcomeBackScreen` tests, 4/4 new `App.tsx` gate tests, plus wiring assertions in `ActiveSessionScreen`/`SessionCompletionScreen` — 154/154 server suite, 477/477 mobile suite, lint/typecheck clean both workspaces.

**DoD:** full lifecycle (create → active → emergency-exit-or-complete) produces correct `rewards_history` rows, verified by integration test against the confirmed bonus spec; 2-device host-drop test correctly migrates within the debounce window; disconnect-and-rejoin test correctly disqualifies Completion Bonus but preserves earned base points.

## Phase 5 — Gamification & Stats (Screens 11–12)
Prerequisites: Phase 4 (needs real `rewards_history` data).

- [x] **Retention-strategy analysis + gamification re-spec** (product-direction pivot — see ARCHITECTURE.md §1/§9, now resolved) — `docs/RETENTION_STRATEGY.md`: seven consumer-app retention patterns rethought against the specific "reward starting a session, not lingering in-app" goal, each with an explicit adopt/reject/defer verdict. **Adopted:** streaks (48h rolling grace, forgiving any-presence counting), milestones (6-tier session-count ladder, same counting rule), and one new mechanic — a visible milestone progress bar on Home (the goal-gradient effect; a bare after-the-fact announcement was leaving the effect on the table). **Rejected outright, not deferred:** variable/randomized rewards (trains checking, not session-starting; also a real loot-box-adjacent ethical/regulatory line), dishonest endowed-progress framing, a parallel level/XP ladder (duplicates streaks+milestones — clutter past that point). **Deferred pending infrastructure that doesn't exist** (new backlog entries below, not silently dropped): push notifications, social/leaderboard features. `ARCHITECTURE.md` §1/§9 rewritten accordingly (banners removed, Keep/Reject/Deferred lists replace the old Keep/Avoid pair).
- [x] **Phase 5 schema migration** (`supabase/migrations/20260728150000_phase5_gamification_and_stats.sql`) — `user_streaks`, `user_stats`, `user_stats_daily`, `milestones` (seeded, 6 tiers per `docs/RETENTION_STRATEGY.md` §2), `user_milestones` created; `users.timezone` (client-reported IANA zone, extends the existing column-scoped update grant) and `session_participants.stats_applied_at` (the exactly-once idempotency key task 3 checks) added. Three deliberate deviations from `DATABASE.md`'s original blueprint, each documented inline in the migration and in `DATABASE.md`: `user_stats.sessions_disconnected` (a third exit-reason counter the blueprint missed), `user_streaks.last_session_day` (stores the resolved local day rather than re-deriving it), `milestones.slug` (an i18n key — `name` alone would be a hardcoded UI string). RLS: the four per-user tables are own-rows-only (`user_id = auth.uid()`, same reasoning as `rewards_history`); `milestones` is global reference data, select-only for every authenticated user. `service_role` gets `select, update` on `user_streaks` only (for the task 5 expiry job's direct write) — every other write happens inside the task 3 `SECURITY DEFINER` function, which needs no table grant, same precedent as `join_session()`. 108/108 pgTAP passing (`supabase/tests/phase5_gamification_test.sql`, 28 new: table/column shape, RLS denies cross-user reads, milestone seed ladder, grants reject direct client writes).
- [x] **`apply_session_stats()` Postgres function** (`supabase/migrations/20260728160000_create_apply_session_stats_function.sql`) — streak calculation, milestone crossing detection, and `user_stats`/`user_stats_daily` write-through folded into one atomic `SECURITY DEFINER` entry point (same precedent as `join_session()`/`rejoin_session()` — read-modify-write from two independent Node call sites can't be made safe in Node alone). Locks the `session_participants` row first (exactly-once guard via `stats_applied_at`), resolves the participant's local day (`users.timezone`, UTC fallback on a garbage zone string so one bad row can't 500 session-end), accumulates lifetime + daily aggregates, advances the streak (any-presence counting, forgiving by design per `docs/RETENTION_STRATEGY.md` §1 — first session / same-day dedupe / within-grace advance / grace-exceeded reset / out-of-order-never-moves-backwards, all five branches pgTAP-covered), and crosses milestones via `insert ... on conflict do nothing returning` (naturally idempotent, handles multi-tier crossing in one session, each crossing writing its own `rewards_history` row so `user_stats.total_points` and `sum(rewards_history.points)` stay equal by construction). 130/130 pgTAP passing (`supabase/tests/apply_session_stats_test.sql`, 22 new) — **both DoD boundaries asserted directly: a 47h gap advances the streak, a 49h gap resets it to 1**, plus same-day no-double-count, out-of-order-doesn't-perturb, timezone-crosses-a-day-boundary, and the total_points↔rewards_history invariant after a 5-session milestone crossing.
- [x] **Node wiring into both finalization paths** — `end-session.ts` calls `store.applySessionStats(sessionId, userId, endedAt, STREAK_GRACE_HOURS)` for every newly-finalized participant (deliberately excludes already-finalized emergency-exit rows — their stats already applied at their own exit moment), and `finalize-emergency-exit.ts` (which gained an injectable clock, matching `end-session.ts`'s pattern) does the same inline. `sessions-store.ts` gains `applySessionStats()` as a thin `client.rpc()` wrapper, same shape as `joinSession`/`rejoinSession`. **Real bug caught during this task, not by any unit test**: the Phase 5 schema migration granted `service_role` `select, update` on `user_streaks` only — `user_stats`/`user_stats_daily`/`user_milestones`/`milestones` had no `service_role` grant at all, so the new integration test's own read-back queries (and any future admin/debug tooling) silently got `null` back. The exact "Phase 2 lesson" (service_role bypasses RLS policies, never grants) recurring in a migration that documented that very lesson — caught only because the integration test asserted against the real Data API rather than trusting the pgTAP suite (which runs as `postgres` and never exercises `service_role`'s actual grants). Migration amended (not yet committed) rather than layering a follow-up fix, since this is same-session iteration. New `apps/server/integration/gamification.integration.test.ts`: create→join→end accumulates `user_stats`/`user_stats_daily`/`user_streaks` for both participants and proves the `stats_applied_at` guard + `total_points == sum(rewards_history.points)` invariant against the real stack; a second `/end` call is rejected and stats provably don't move; the emergency-exit path accumulates independently. 157/157 server unit tests (10 new), 10/10 integration tests (2 new), lint/typecheck clean.
- [x] **Streak expiry job** — `apps/server/src/modules/stats/streak-expiry.ts`'s `runStreakExpiry()`, the same testable-core-plus-`setInterval` split as the Phase 4 sweep worker (`server.ts` gains a second, independent `setInterval` on the new `STREAK_EXPIRY_INTERVAL_SECONDS=300` cadence — deliberately slower than the session sweep, since there's no debounce-window concern the way host migration has). Delegates to a new `SessionsStore.expireStreaks(asOf)` — a single scoped `UPDATE` (`service_role`-granted directly, not a `SECURITY DEFINER` function, since zeroing an already-expired streak has no atomicity concern the way `apply_session_stats()`'s cross-table read-decide-write does) that zeroes `current_streak` where `streak_grace_expires_at < asOf`, never touching the monotonic `longest_streak`. 5/5 new unit tests (against the shared fake store, extended with a real in-memory `streaks` model) covering the expire/no-op/idempotent/multi-user cases. 162/162 server suite green, lint/typecheck clean.
- [x] **Mobile: timezone reporting + stats/history repository reads** — `apps/mobile/src/services/user-profile.ts`'s `reportTimezoneIfChanged()` writes the device's IANA zone (`react-native-localize`'s `getTimeZone()`) to `users.timezone` on auth, wholly fail-open (an outer try/catch guarantees it never rejects, so `App.tsx` fires it un-awaited with no `.catch()` — same "intentionally not awaited" framing as the onboarding/permission handlers already there), and skips the write entirely when an `AsyncStorage`-cached value already matches (no redundant write every app start). New `apps/mobile/src/services/stats-repository.ts` (sibling to `session-repository.ts`, split out since it's a different feature domain — user-level aggregates and cross-session history, not one session's own data): `fetchUserStats`/`fetchUserStreak` (`maybeSingle` — a brand-new user has no row yet, apply_session_stats() creates it lazily), `fetchDailyStats` (day-bucket rows for the 7-day chart, caller supplies the local-day lower bound so the function stays pure/deterministic), `fetchMilestoneProgress` (combines the global `milestones` ladder with the user's own `user_milestones` crossings via `Promise.all`), `fetchSessionHistory` (keyset-paginated, `session_participants!inner` embedded join filtered to the user — PostgREST always returns the embed as an array even though the `(session_id, user_id)` unique constraint guarantees exactly one match, normalized to a flat row here so callers never need to know that; Solo/Group/All maps to `.eq('type','solo')` / `.in('type', ['dynamic_qr','static_qr'])` / no filter). 23 new mobile unit tests (7 timezone-reporting incl. the outer-fail-open-net case, 17 repository), `App.tsx` wiring proven not to break either App-level spec despite their mocks lacking `getTimeZone`/`.from` (exactly the outer-catch's job). 501/501 mobile suite green, lint/typecheck clean.
- [x] **Home summary card** (`apps/mobile/src/screens/HomeScreen.tsx`) — current streak, lifetime points, and a milestone progress bar ("N more sessions to X", the goal-gradient mechanic adopted in task 1), reading via `stats-repository.ts`. Zero-state renders real zeros immediately rather than gating the screen's original two CTAs behind a loading spinner — the fetch fills in once it resolves, in place. Milestone name resolution deliberately avoids an unchecked type assertion on the DB-sourced `slug` (typescript-strictness skill): an exhaustive switch over the known literal i18n keys, falling back to the raw slug for a milestone seeded server-side without a matching translation yet. **Navigation routes deferred, not built here**: `History`/`Stats` aren't added to `RootStackParamList` or `App.tsx` in this task — a route needs a real registered component, and building placeholder screens just to satisfy "add the route" would violate the no-half-finished-implementations rule. Each route lands atomically with its own screen in tasks 8/9 instead — a small, transparent reordering from this task's original title. **Real gap caught, not by HomeScreen's own spec**: `App.spec.tsx`/`App.auth-gate.spec.tsx` both render the real `HomeScreen` as part of proving the gate flow, and neither had mocked the new `stats-repository` module — Home's new fetch call hit an unmocked `getSupabaseClient().from`, throwing inside a `useEffect` in both suites. Fixed by adding the same repository mock (with safe defaults) both suites already use for `session-repository`. 12 new `HomeScreen` tests, 509/509 mobile suite green, lint/typecheck clean.
- [x] **History screen** (`apps/mobile/src/screens/HistoryScreen.tsx`, Screen 11) — Solo/Group/All filter as local state, `stats-repository.ts`'s keyset-paginated `fetchSessionHistory()` re-queried from page one on every filter change (never a client-side re-filter of already-loaded rows, so cursors stay correct per filter). `FlatList` (the first in this codebase — every prior list was small enough for a plain `.map()`) with `onEndReached` guarded against firing while a page is already in flight and against paging past a short last page. Distinct empty-state copy per filter; a load failure shows a retry affordance (re-fetch via a `reloadToken` bump, reusing the same effect rather than duplicating the fetch) instead of a silent blank list. `RootStackParamList`/`App.tsx` gain the real `History` route (registered alongside the screen itself, not preemptively in task 7), reached from a subdued Home text link, not a third competing CTA. 14 new screen tests (i18n proof, all three filters, both empty and error states, retry, pagination cursor correctness, the "no more pages" guard, unauthenticated), 524/524 mobile suite green, lint/typecheck clean.
- [x] **Stats screen** (`apps/mobile/src/screens/StatsScreen.tsx`, Screen 12) — lifetime `user_stats` aggregates, current/longest streak, achieved-milestone list, and a 7-day chart. Chart: seven plain `<View>` bars (no charting library added — none exists in this codebase and the deferred color palette means any library's default styling would need immediate rework), day boundaries from a new pure `getLastSevenLocalDays()` (`services/local-days.ts`, injectable clock, device-local to match the server's `users.timezone` bucketing — its own 4 unit tests cover month/year boundary crossings and the local-vs-UTC distinction directly). All-zero week renders a visible minimum-height stub per bar rather than collapsing to nothing; a single dominant day scales the rest against that day's real max, not a fixed ceiling. The milestone-name translation switch (originally written inline in Task 7's Home screen) was extracted to a shared `i18n/translate-milestone-name.ts` once a second screen needed the identical logic — one switch statement, its own unit test, both screens import it. `RootStackParamList`/`App.tsx` gain the real `Stats` route registered with the screen itself (same pattern as History), reached from a second subdued Home link alongside History's. 29 new tests (5 `StatsScreen`, 4 `getLastSevenLocalDays`, 6 `translateMilestoneName`, 2 new `HomeScreen` — a Home refactor + the Stats link — plus incidental others), 547/547 mobile suite green, lint/typecheck clean.

**DoD:** streak survives a 47h gap, breaks after 49h — both boundaries asserted directly in `apply_session_stats_test.sql` (task 3) via a controlled `p_finalized_at`, independent of wall-clock time. Stats screen sum matches `rewards_history` exactly — proven as the invariant `user_stats.total_points == sum(rewards_history.points)` in both the pgTAP suite (task 3, after a 5-session milestone crossing) and the real integration test (task 4, against the live local stack after base + both bonuses + a milestone); the Stats screen reads `user_stats.total_points` directly and unmodified, so proving the invariant at its two sources proves the screen. **Phase 5 complete** — all 9 tasks (retention analysis, schema, `apply_session_stats()`, both finalization paths wired, streak-expiry job, mobile timezone/repository plumbing, Home summary card, History screen, Stats screen) closed with full TDD coverage: 162 server unit tests, 130 pgTAP assertions, 10 server integration tests, 547 mobile unit tests, all green; lint/typecheck clean across both workspaces throughout.

## Phase 5.5 — Push Notification Infrastructure *(deferred from the Phase 5 retention analysis — `docs/RETENTION_STRATEGY.md` §5)*
Prerequisites: Phase 5 (streak/milestone data must exist before there's anything to notify about).

**Planned and closed 2026-07-30** (see conversation) as an end-to-end phase plan before any code, per the working contract. Owner decisions confirmed before implementation: the streak-risk push fires when `streak_grace_expires_at` is within **6 hours** (owner-chosen — a meaningful fraction of `STREAK_GRACE_HOURS`, late enough that most people who were going to have a session today already did); **no quiet-hours suppression** this phase (send whenever the threshold is crossed — simplicity over perfection, revisit if it's ever a real complaint); `device_tokens` is **one row per (user, platform)**, newest wins on reinstall (a second same-platform device steals the slot — accepted MVP limitation). Same posture as Phase 2/6 attestation: no Firebase project or Apple Push credentials exist yet, so this ships as a fully-wired, tested, **inert** pipeline — real schema, real atomic claim logic, real dispatch job, only the delivery source (`NotificationSender`) and native token acquisition (`pushRegistration`) are placeholders. New scope surfaced during planning, not silently bundled: composing notification text server-side is the server's first-ever user-facing string, so a `users.locale` column + `reportLocaleIfChanged()` were added to honor `CLAUDE.md`'s binding i18n rule.

- [x] **`device_tokens` table + `users.locale` column** (`supabase/migrations/20260730000000_push_notifications_device_tokens.sql`) — `device_tokens` (`user_id`, `platform` check `'android'|'ios'`, `token`, `created_at`, `last_seen_at`, **`unique (user_id, platform)`** — the upsert target that makes a reinstall replace rather than duplicate a row), RLS own-rows-only, `service_role` gets `select` only (registration is always a direct authenticated client write — mirrors `timezone`, not money-equivalent). `users.locale text` nullable, extends the existing column-scoped `update` grant alongside `display_name`/`avatar_url`/`timezone` (never table-wide — would reopen `role`). 15 pgTAP assertions (`supabase/tests/phase5_5_push_notifications_test.sql`): shape, RLS (an insert under a different `user_id` throws; an update targeting another user's row silently affects zero rows — an RLS `USING` clause filters, it doesn't raise, the real lesson this task's own test run caught), the upsert-on-conflict actually replaces (not duplicates) a row.
- [x] **`NotificationSender` seam + streak-risk copy** — `apps/server/src/modules/notifications/notification-sender.ts` (`NotificationSender` interface + `unconfiguredNotificationSender`, same shape/rationale as `unconfiguredAttestationProvider`: no FCM/APNs credentials exist, so the placeholder keeps the *pipeline* real and tested while only delivery is a stand-in). `streak-risk-copy.ts` — the server's first-ever localized user-facing text: a small `{en, he}` lookup + `resolveNotificationLocale()` (fails open to `en`), body copy deliberately generic (no interpolated hour count — the job fires on a threshold crossing, not a live countdown). 10 unit tests.
- [x] **Streak-risk claim function + dispatch job** (`supabase/migrations/20260730000100_streak_risk_notification_claim.sql`) — `user_streaks.risk_notification_sent_for` (stores *which* deadline was last notified for, not a boolean, so a fresh deadline is always eligible again) + `claim_streak_risk_notifications(p_now, p_window_hours)`, a single atomic `UPDATE ... RETURNING` (the same race a naive "select then send then mark" would have — an overlapping poll tick re-notifying before the "already notified" write lands — closed the same way `join_session()`/`apply_session_stats()` close theirs). Table aliased and every column qualified from the start, avoiding Phase 6's `RETURNS TABLE` OUT-parameter collision proactively. New `apps/server/src/modules/notifications/notifications-store.ts` (its own bounded-context store, not folded into `SessionsStore`) + `streak-risk-notifier.ts` (`runStreakRiskNotifications` — claim → look up targets → dispatch, each send wrapped so one failure never aborts the batch). New constants `STREAK_RISK_NOTIFICATION_WINDOW_HOURS=6`, `STREAK_RISK_NOTIFICATION_INTERVAL_SECONDS=300`; wired into `server.ts` as a third `setInterval` poller alongside the sweep/streak-expiry jobs. 6 new pgTAP assertions + 6 unit tests (fake store/sender) + 4 real integration tests against the live local stack (`integration/notifications.integration.test.ts` — a real send attempt at a registered token, no re-notify on an immediate second tick, a claimed-but-tokenless user consumes its window with no send, a fresh deadline is eligible again after already being notified once).
- [x] **Mobile push-registration seam + wiring** — `apps/mobile/src/services/push-registration.ts` (mirrors `native-sign-in.ts` exactly: deterministically reports `'unavailable'`, no FCM/APNs SDK linked yet, fabricating a token would fake a capability the app doesn't have). `user-profile.ts` gains `registerPushTokenIfChanged()` (upserts `device_tokens` on `(user_id, platform)`, a no-op in practice today since `pushRegistration` never yields a real token) and `reportLocaleIfChanged()` (writes `users.locale`, reusing `resolve-device-locale.ts`'s existing resolution rather than inventing new detection). Both wired into `App.tsx`'s auth-gate transition alongside the existing `reportTimezoneIfChanged`. 14 new unit tests.

**Verification**: 240 server unit tests, 175 pgTAP assertions, 21 real integration tests against the live local Supabase stack, 631 mobile unit tests. Lint and typecheck clean across both workspaces throughout.

**DoD:** a user whose streak grace expires within 6 hours and hasn't had a session since receives exactly one push before it lapses — proven at the mechanism level (the claim function fires exactly once per grace-deadline instance, the dispatch job attempts exactly one send per registered device) since no real send credential exists yet to prove actual on-device receipt; token registration survives app reinstall (new token replaces old on the same platform, no duplicate sends) — proven by the upsert-on-conflict pgTAP assertion. Not machine-verifiable → `docs/MANUAL_QA.md`: real FCM/APNs credentials, native SDK linking, and an actual on-device push receipt are all still outstanding — this phase ships the pipeline, not a working push to a real phone.

**DoD:** a user whose streak grace expires within N hours and hasn't opened the app receives exactly one push before it lapses; token registration survives app reinstall (new token replaces old, no duplicate sends).

## Phase 6 — Hardening & B2B

**Planned 2026-07-28** (see conversation) as an end-to-end phase plan before any code, per the working contract — four crux design decisions were surfaced and confirmed before implementation: static QR is a **venue-scoped long-lived token** (find-or-create the venue's active session, not a per-session token); Play Integrity/App Attest enforcement ships **built but off** (`ATTESTATION_ENFORCEMENT_ENABLED=false`) since no real credentials or monitor-mode data exist to threshold against; **root/jailbreak detection merges into the attestation trust tier** as one mechanism, not a second client-side check (a rooted device is exactly what Play Integrity's own verdict is meant to catch, and a client-side probe can be lied to by the device it's checking); B2B metrics are **per-venue, trailing 30 days, aggregates only**. Two real gaps were found during planning that the original backlog wording never named and are folded in below: a non-participant couldn't read any session details before joining (RLS correctly denies it — this is why "late-join details" needed a whole new server endpoint, not a client fetch), and `ActiveSessionScreen` never navigated away when a session ended, stranding every non-host participant on a dead screen.

Prerequisites: Phase 5.

- [x] **Role authorization primitive** *(new foundation, not itself a named backlog line — nothing role-gated below is buildable without it)*: `apps/server/src/modules/users/users-store.ts` (`getUserRole()`, the server's first-ever read of `public.users.role`) + `apps/server/src/middleware/require-role.ts` (`createRequireRole()`, mounted after `requireAuth`). Deliberately a per-request DB lookup, not a JWT custom claim — a claim would go stale until the caller's token next refreshes, wrong for a role flipped by hand in Supabase Studio. New migration `20260729000000_grant_users_select_service_role.sql` (`select`-only — Node never gets a write grant on `role`; the escalation-proof column-scoped grant from Phase 1 is untouched). **This *is* the "Verified Host manual approval flow"** — `docs/RUNBOOK_VERIFIED_HOST.md` documents the exact SQL runbook; per `CLAUDE.md`'s already-locked decision ("granted manually by flipping a DB flag... no in-app application flow yet") no admin screen exists or is planned.
- [x] **Venues: grants, ownership, create/list API** — the original venues migration granted `authenticated` `select` only and **no `service_role` grant at all**, the exact "permission denied" trap `create_sessions_core.sql` already documents for a different table; new migration `20260729000200_venues_ownership_and_grants.sql` fixes the grant and tightens the read policy from `using (true)` to owner-only (nothing depended on the wide policy — a joiner learns a venue's name through the new preview endpoint, not a direct read). `apps/server/src/modules/venues/` gains `venues-store.ts`, `venues.router.ts` (`POST /venues`, `GET /venues`, both `requireAuth` + `requireVerifiedHost`). Closed the live `venue_id` hole in `sessions.router.ts`: a session tagged with a `venue_id` now 403s `venue_not_owned` / 404s `venue_not_found` unless the caller owns it — **applies to every session type** (owner decision), not just `static_qr`.
- [x] **Static QR: venue token + atomic join-by-venue** — migration `20260729000300_static_qr_venue_token.sql` adds `venues.qr_token`/`qr_token_issued_at` (no expiry, unlike a session's 15-minute token — the whole point of a printed code), `chk_static_qr_has_venue`, a partial unique index (`one active static_qr session per venue`), and `public.join_venue_session()` (mirrors `join_session()`'s row-locking shape, resolving "the venue's active session" and joining it in one atomic statement). **Two real bugs caught by the actual pgTAP/Postgres run, not by inspection**: `RETURNS TABLE(outcome, session_id)` implicitly declares `session_id` as an OUT parameter, colliding with `session_presence_intervals.session_id` when left unqualified ("column reference is ambiguous"); and `RETURN QUERY` does **not** exit a PL/pgSQL function the way a plain `RETURN` does — a `no_active_session` branch was silently falling through into the join logic below and appending a second, contradictory `'joined'` row. A **third** real gap, also only caught by writing the actual integration test: `join_venue_session()` never re-checked the token against the venue's *current* `qr_token`, so regenerating a venue's code wouldn't have invalidated the old printout — fixed to mirror `join_session()`'s own "checked twice" shape. New `VENUE_SESSION_MAX_PARTICIPANTS=200` constant (owner decision: a verified host's foot traffic needs a materially higher cap than a friend group's 50, kept a config constant rather than a per-venue DB column for MVP simplicity). `POST /sessions/join-venue`, `POST /venues/:id/qr/regenerate`.
- [x] **Mobile: profile/role read + venue UI + static_qr** — `services/user-profile.ts` gains `fetchUserProfile()` (was write-only before this phase); new `state/profile-store.ts` (re-hydrated fresh every authenticated session, never persisted, so a manual runbook role flip is never masked by a stale cached value). New `VenueManagementScreen.tsx` (create a venue, view/regenerate its printed code — rendered as plain text, same limitation `ActiveSessionScreen`'s existing dynamic_qr card already carries, since no QR-rendering library exists in this codebase). `CreateSessionScreen.tsx` gains `static_qr` (verified-host-gated) with a venue picker, and its type→i18n-key mapping was fixed from a hardcoded two-way ternary (which would have silently mislabeled `static_qr`) to an explicit `Record`.
- [x] **Session preview endpoint** — `POST /sessions/preview` (`apps/server/src/modules/sessions/preview-session.ts`): tries the session-token HMAC first, then the venue-token HMAC, so the client never needs to know which kind it scanned. Returns aggregates only (participant count, elapsed minutes, venue name, server-computed `completionBonusAvailable`) — never identities, and `completionBonusAvailable` is computed server-side only (Money-Equivalent Logic Rule: a statement about reward attainability is never mirrored client-side). This is the endpoint that makes "late-join details" possible at all, since `is_session_participant()`'s RLS correctly denies a non-participant a direct read.
- [x] **B2B metrics endpoint** — `GET /venues/:id/metrics`, gated by `requireVerifiedHost` **and** ownership (checked inside `get-venue-metrics.ts` itself — role alone would let any verified host read any venue's numbers). New Postgres function `get_venue_metrics()` (one SQL function rather than several Node round trips averaged in-process — PostgREST's query builder has no native `avg()`/`count()` aggregate support the way a plain SQL function does). New `VENUE_METRICS_WINDOW_DAYS=30` constant.
- [x] **B2B dashboard screen** — `VenueDashboardScreen.tsx`: auto-selects a lone venue, offers a picker for multiple; fetches on mount plus an explicit "Refresh" link (a `useFocusEffect` refetch-on-focus was deliberately dropped — it needs a real `NavigationContainer` to fire, which would have meant introducing a new test-setup pattern this screen's own tests don't otherwise need). Aggregates only, no individual customers ever rendered.
- [x] **Edge-case screens pass** — new shared `components/StatusBanner.tsx` (the app's first shared UI primitive besides `QrCodeCamera`; three call sites — blocker violation, host-migration toast, offline — differentiated by weight/placement, never colour, since the palette stays deferred). `ActiveSessionScreen` gains a dedicated offline banner for `participant_reconnecting`/`degraded_offline`, **and now navigates (`reset`) into Session Completion the instant status becomes `completed`/`force_terminated`** — the stranded-participant bug found during planning. `SessionDetailsScreen` rebuilt on the preview endpoint: real details before joining (participant count, elapsed time, venue name), a late-join note once the completion bonus is provably out of reach, and a specific recovery affordance per failure code (Scan again / Back to Home / Retry) — previously every one of six failure codes rendered an identical gray line with no way out. **A real functional gap found while doing this rebuild**: nothing in the mobile app could actually join a `static_qr` session via a venue token at all — `api-client.ts` gained `joinVenueSession()`, and the preview response gained a `tokenKind` discriminator (`'session' | 'venue'`) so `SessionDetailsScreen` knows which endpoint to call. Fixed the rejoin dead-end from `WelcomeBackScreen`: a failed rejoin now has a real recovery action instead of nowhere to go.
- [x] **Root/jailbreak detection + Play Integrity/App Attest enforcement — merged into one device-trust-tier mechanism** (owner decision; two backlog lines above become one implementation, recorded here rather than silently dropping either): new `apps/server/src/modules/attestation/trust-tier.ts` (`verdictToTrustTier()` — a narrow denylist, since the real verdict vocabulary is still unknown with no credentials; fails open to `'trusted'` for anything unrecognized, including today's `not_configured`/`provider_error` placeholders — and `applyEnforcementPolicy()`, the one place `ATTESTATION_ENFORCEMENT_ENABLED` actually takes effect, gating once at write time rather than threading the flag through every downstream reader). Migration `20260729000500_device_trust_tier.sql` adds `device_trust_tier` to `session_presence_intervals`/`session_participants` (both `not null default 'trusted'`) and re-creates `apply_session_stats()` with a 5th `p_device_trust_tier` parameter that skips the streak-advance branch only (lifetime stats/milestones are unaffected — "exclude from group bonus **and** streak only"). **A real Postgres gotcha caught by the actual migration run**: `CREATE OR REPLACE FUNCTION` with an *added* parameter creates a new overload, not a replacement, leaving the old 4-arg function ambiguously resolvable alongside the new 5-arg one — fixed with an explicit `DROP FUNCTION IF EXISTS` first. `points/group-bonus.ts` gains two guards mirroring the existing `blockerReadyAt` gate exactly (an unverified interval contributes no timeline event and forfeits its own eligibility, base points unaffected). New `SessionsStore.markDeviceTrust()` (a fire-and-forget follow-up write after join, exactly `markBlockerReady`'s precedent), wired from `sessions.router.ts` using whatever attestation verdict the client already optionally supplies. **Ships fully wired and tested with enforcement off** — flipping `ATTESTATION_ENFORCEMENT_ENABLED` to `true` once real credentials and monitor-mode data exist is the only remaining step.

**Verification**: 224 server unit tests, 154 pgTAP assertions, 17 real integration tests against the live local Supabase stack (the level that caught all four bugs listed above — none were visible from code inspection or from the mocked-store unit suites alone), 618 mobile unit tests. Lint and typecheck clean across both workspaces throughout.

**DoD:** every edge case has an explicit screen/state, covered by a test or documented manual QA step — met (`docs/MANUAL_QA.md` tracks the handful of on-device-only items: a real printed venue QR scan, the B2B screen on real hardware, RTL on every new screen). B2B screen shows correct live metrics for a verified host account — met and proven end-to-end by a real integration test (create venue → live customer count → session ends → 30-day aggregate), including that a *different* verified host is correctly denied another host's numbers.

## Phase 6.5 — Social & Comparison Surfaces *(deferred from the Phase 5 retention analysis — `docs/RETENTION_STRATEGY.md` §4)*
Prerequisites: Phase 6.

**Planned and closed 2026-07-31** (see conversation) as an end-to-end phase plan before any code, per the working contract. Owner decisions confirmed before implementation: friend model is **mutual request/accept** (symmetric, opt-in both ways, not a one-directional follow); discovery is **search by username** (the owner's explicit call, overriding two proposed alternatives — a shareable code or email search — meaning this phase also had to add a `username` concept, which didn't exist before); activity data uses the **same forgiving "any session with presence" rule** streaks/milestones already use. **A friends-only leaderboard ranked by total lifetime points was added mid-review**: the initial plan defaulted to no leaderboard (`ARCHITECTURE.md` §9's original comparison-anxiety rejection), the owner explicitly reversed that at the plan-approval step and chose points over streak-length or both as the ranking metric — exactly the kind of previously-rejected pattern the 2026-07 product-direction pivot anticipated reopening deliberately, with a real stop-and-ask before committing.

- [x] **`users.username` + signup-trigger generation** (`supabase/migrations/20260731000000_users_username.sql`) — unique, not-null column; `handle_new_user()` re-created (same signature — a trigger function takes no arguments, so no Phase 6 overload-collision risk here) to derive a lowercase-alphanumeric username from `display_name`, retry-on-`unique_violation` rather than a pre-check-then-insert (two concurrent signups sharing a display name could otherwise both pass the same "is this free" check before either commits — the same race shape as `claim_streak_risk_notifications()`/`join_venue_session()`). Extends the existing column-scoped grant (schema-ready for a future "edit your username" screen; none built this phase). 180/180 pgTAP passing.
- [x] **Friend graph schema + atomic request/accept functions** (`supabase/migrations/20260731000100_friend_graph.sql`) — `friend_requests` (ephemeral: accepted → becomes a friendship, declined/cancelled → just deleted, never a persisted terminal status) and `friendships` (canonical `user_id_a < user_id_b` ordering — exactly one row per pair regardless of who sent the original request, the same push-the-invariant-into-the-schema principle as Phase 6's partial unique index). `send_friend_request()`/`respond_to_friend_request()` are Node-only atomic functions; a mutual double-request (both users request each other before either responds) auto-resolves to a friendship immediately. Both functions delete any pending request in **either** direction before inserting a friendship and wrap the insert against `unique_violation` — closing a real concurrent-opposite-direction race identified while writing the migration (two truly simultaneous calls could each discover "no reverse request yet" before either commits). 209/209 pgTAP passing (`supabase/tests/phase6_5_social_test.sql`, 29 new).
- [x] **Node API: search + friend requests** (`apps/server/src/modules/friends/`) — `GET /friends/search` (min 2-character query, a proportionate enumeration mitigation, not full abuse-prevention infra), `GET /friends/requests`, `POST /friends/requests`, `POST /friends/requests/:id/respond`. `public.users`' RLS stays "own row only" — unwidened; search and relationship-state computation are Node-computed (`service_role`), same posture as Phase 6's venue metrics/session preview. 11 router tests.
- [x] **Friends leaderboard: points + "active today" aggregate** — `FriendsStore.listFriends()` returns the caller plus every friend, each with `user_stats.total_points` (the same authoritative value the Stats screen already shows) and a server-computed `hadSessionToday` boolean (`user_streaks.last_session_day` compared against **that friend's own local day** — new `local-date.ts`, an `Intl.DateTimeFormat('en-CA', {timeZone})` pure function, UTC-fallback on a garbage zone, mirroring `apply_session_stats()`'s own posture without needing a timezone library), pre-sorted by points descending. Only these two fields ever cross the boundary — never `current_streak` or anything else `user_stats`/`user_streaks` hold. 7 unit tests (the pure local-date functions) + 4 real integration tests.
- [x] **Mobile: Friends leaderboard screen** — `FriendsScreen.tsx` (search with per-result relationship state, incoming-request accept/decline, a ranked leaderboard with a Remove action) reached via Home's new `friendsCta` link (available to every user, not role-gated, unlike the venue screens). Unfriending is a **direct client-side RLS delete** (`friends-repository.ts`'s `removeFriend()`), no Node round trip — mirrors the "no cross-row invariant to protect on removal" reasoning already used elsewhere. 8 screen tests + 2 repository tests + 11 new `api-client.ts` tests.
- [x] **Docs close-out** — this entry, `docs/DATABASE.md`, `docs/ARCHITECTURE.md` §9, `docs/RETENTION_STRATEGY.md` §4.

**Verification**: 260 server unit tests, 209 pgTAP assertions, 25 real integration tests against the live local Supabase stack, 648 mobile unit tests. Lint and typecheck clean across both workspaces throughout.

**DoD:** a user can see friend activity without any friend's individual session data being exposed beyond what the privacy design explicitly allows — met: only `totalPoints` (the one number deliberately exposed, needed for the leaderboard the owner asked for) and a coarse `hadSessionToday` boolean ever cross the boundary, proven by a real integration test asserting a friend's actual earned points and same-day activity, never their streak or raw session history. Adding/removing a friend is instant and RLS-verified from both sides — met: `send_friend_request()`/`respond_to_friend_request()` are atomic (pgTAP-proven against concurrent/mutual races), and unfriending is a direct RLS-scoped delete proven end-to-end by a real integration test using a per-user-scoped client, exactly the mobile app's own write path.

**DoD:** a user can see friend activity without any friend's individual session data being exposed beyond what the privacy design explicitly allows; adding/removing a friend is instant and RLS-verified from both sides.

## Phase 7 — Release Prep
Prerequisites: Phase 6 (Phase 6.5 also complete, sitting ahead of Phase 6 — historical note: this was recorded when a `dev` branch existed; the convention now branches off `main` and merges straight back, see `CLAUDE.md`).

**Planned 2026-07-28** (see conversation) as an end-to-end phase plan before any code, per the working contract — this phase touches nearly every item on `CLAUDE.md`'s "known gaps" list, so each one was surfaced and confirmed before task-writing rather than assumed. Locked decisions:
- **Production API hosting:** a PaaS (Railway/Render/Fly.io-style), not serverless — the sweep/streak-expiry/streak-risk-notification workers are long-running `setInterval` processes (Phase 4/5/5.5 decisions) that would need real rearchitecting to run on a stateless serverless platform; a PaaS runs them as-is.
- **iOS build:** a cloud macOS CI runner (GitHub Actions `macos-latest`) gets added to `ci.yml` this phase — the first real compile of the Swift blocker code (`apps/mobile/ios/LockalTime/Blocking/`, written since Phase 3 task 3.6, never built) happens here, not deferred further.
- **Observability:** minimal crash/error monitoring (Sentry or equivalent) added to both workspaces before shipping to real users — a small, well-scoped addition given how much native code has never run on real hardware.
- **Color palette:** the real palette replaces `DESIGN_GUIDELINES.md`'s deferred neutral-grayscale tokens in this phase, not a later one.
- **Legal content:** placeholder ToS/Privacy Policy drafted this phase (covering the app's actual data footprint — session/points data, device tokens, attestation raw responses, friend graph, username, venue data; explicitly no geolocation/contacts) for owner review, not a lawyer-reviewed final version.
- **Data retention:** account data is retained for the life of the account and cascade-deleted on account deletion (matches the schema's existing `on delete cascade` chain) — the policy line the privacy label/policy state.
- **Push + attestation credentials:** obtaining real Firebase/APNs credentials and Play Integrity/App Attest credentials is in scope this phase (owner-actioned, same shape as the existing Family Controls entitlement task) rather than shipping v1 permanently inert on both.
- **Launch strategy:** TestFlight + Play internal testing precede public submission — this app has an unusually large "manual QA pending" list, and a beta ring is what actually closes it against real hardware.

Two real gaps surfaced during planning that the original five-line draft never named, folded in below: **no account-deletion flow exists anywhere in the app** — a hard App Store (guideline 5.1.1v) and Play Store requirement for any app that supports account creation. And: the Express API has no rate-limiting or security headers (`helmet`) at all — fine for a dev-only local stack, not fine for a public production endpoint.

Explicitly out of scope for this phase (confirmed, not overlooked): B2B self-serve monetization / a real Verified Host application flow (`ARCHITECTURE.md` §10 already calls this "V2, explicitly deferred"); a marketing site/roadmap beyond the store listing itself; product-roadmap milestones (beta/launch dates) — a scheduling decision, not an engineering task.

Task order (grouped by track; a track's tasks are prerequisites for later tracks where noted):

**Infra & deployment**
- [x] Staging Supabase project (`LockalTime-staging`, decided in `CLAUDE.md`, not yet created): provision it, push every migration, and specifically re-verify `service_role` grants land — the exact miss `docs/MANUAL_QA.md` already documents twice for the production project — runbook + `scripts/verify-service-role-grants.sql` written and verified against the local stack (37/37 grants present); actual project creation is owner-actioned (no Supabase login in this environment) per `docs/DEPLOYMENT.md`
- [x] Provision production `apps/server` hosting on a PaaS; wire real env vars/secrets (Supabase service-role key, QR-signing HMAC secret, JWKS config) outside the repo; confirm the sweep/streak-expiry/streak-risk-notification pollers run continuously under it — Railway chosen (owner decision); `apps/server/Dockerfile` written and verified with a real local `docker build` + `docker run`; env var list + setup steps in `docs/DEPLOYMENT.md`; actual account/deploy is owner-actioned
- [x] API hardening: `helmet` (security headers) + rate-limiting on public endpoints, prioritizing the highest-abuse surface (`POST /sessions`, `/sessions/join`, `/sessions/join-venue`, `/friends/search`)
- [x] CI: staging-deploy workflow (GitHub Actions secrets for the staging Supabase project + PaaS deploy token), triggered on merge to a release branch or manually — `.github/workflows/staging-deploy.yml` (test gate → migrate staging → verify grants → deploy to Railway); needs the owner-actioned secrets to actually run end-to-end

**iOS build** *(Apple Developer Program enrollment confirmed active by the owner, 2026-07-29 — see Credentials track)*
- [x] Cloud macOS CI: add a `macos-latest` job to `ci.yml`, perform the one-time Xcode project wiring `docs/MANUAL_QA.md` already documents (add `Blocking/` Swift files + bridging header, App Groups/Family Controls capabilities, the `DeviceActivityMonitorExtension` target, `pod install`) — **the first real `xcodebuild` compile of this codebase, and it's green** (`BUILD SUCCEEDED`) — scripted via the `xcodeproj` gem (`apps/mobile/ios/scripts/wire-blocking-target.rb`, idempotent) instead of by hand; `ci.yml`'s new `ios-build` job runs it + `pod install` + an unsigned Simulator `xcodebuild`. Reached across 10 real CI-triggered rounds, each catching one genuine, distinct bug: an empty `PRODUCT_NAME` on the extension target ("Multiple commands produce '.appex'"), manually-constructed `PBXGroup`/`PBXVariantGroup` objects not correctly resolving their path when nested under this project's pre-existing `LockalTime` group (three separate instances of this, one per group — fixed by anchoring every group/file the script creates directly at `project.main_group` with an explicit full path instead), two iOS-16-only FamilyControls/ManagedSettings APIs called at this project's 15.1 deployment target (`clearAllSettings()`, `requestAuthorization(for:)` — both given iOS-15-compatible fallbacks), a missing `import React` (this RN 0.86 template is Swift-native/CocoaPods-modular-headers, confirmed by `AppDelegate.swift`'s own working pattern — not the classic bridging-header-only Objective-C interop the Blocking/ files were originally written assuming), and a `Result<Void, Error>`-vs-`Error?` completion-handler type mismatch. Signed device/App Store builds still need the real Family Controls entitlement approval + a distribution cert (Credentials track, both owner-actioned)
- [x] Swift code-review pass `docs/MANUAL_QA.md` already flags as a prerequisite before device time (the `scheduleMonitoring` midnight-spanning-session edge case especially) — found `DeviceActivitySchedule` truncates any session over 24h to `(duration mod 24h)` (only hour/minute/second survive), and that the assumed "fixed sessions are bounded by product design" invariant wasn't actually enforced anywhere; fixed with `FIXED_SESSION_MAX_DURATION_MINUTES` (`apps/server/src/config/constants.ts`), validated at the API boundary
- [ ] App Store Screen Time entitlement — confirm approval status, or document fallback plan (carried over from the original draft)
- [ ] iOS release signing: distribution certificate + provisioning profile, once the entitlement is confirmed approved (Apple Developer enrollment itself is confirmed)

**Android release**
- [x] Restore release ABIs: `apps/mobile/android/gradle.properties`'s `reactNativeArchitectures` back to the real device set (at least `arm64-v8a,armeabi-v7a,x86_64`) — currently pinned to `arm64-v8a` only for fast USB-connected dev builds (carried over from the original draft) — restored as the default for every build (fast single-device dev loop moved to `--active-arch-only`); verified with a real local `./gradlew assembleDebug` (all 3 ABIs present in the APK) and `assembleRelease`
- [x] Android release signing: generate a real release keystore, wire the Gradle signing config, store keystore/passwords as CI/PaaS secrets, never in the repo — `build.gradle` reads `android/keystore.properties` (gitignored) if present, falls back to debug signing otherwise (verified: `assembleRelease` succeeds either way); real keystore generation is owner-actioned (a signing key is a sensitive, hard-to-reverse artifact — runbook in `docs/DEPLOYMENT.md`, `keystore.properties.example` template committed)
- [ ] Confirm physical Android device availability/status — nearly all of Phase 3's native-enforcement manual QA (actual blocking, boot persistence, permission round-trips) has never run on real hardware; this is a release-blocking prerequisite, not optional polish

**Store readiness, legal, branding**
- [x] Draft placeholder ToS + Privacy Policy (mine to write this phase, per the locked decision above); decide and stand up a stable hosting URL for them — `apps/server/src/modules/legal/legal-content.ts`, served plain-text (no auth) at `GET /legal/terms`/`GET /legal/privacy`; the stable URL is just wherever the API is deployed (Railway), no separate marketing domain needed for placeholder content
- [x] In-app ToS/Privacy Policy acceptance during onboarding/auth (currently no acceptance UI exists anywhere) — a passive disclosure with links on `AuthScreen`, not a blocking checkbox (email-OTP handles sign-up/sign-in identically, so there's no distinct "signup" moment to gate); `users.tos_accepted_at` (direct client write, conditioned on `is null` so it's stamped exactly once) recorded via `acceptTosIfNeeded()` on every authenticated session
- [x] **Account deletion flow** (new scope, not polish): a Node endpoint or direct RLS-permitted path that deletes the `auth.users` row and lets the schema's existing cascade chain clean up every dependent table, plus a mobile confirmation screen — `DELETE /account` (`apps/server/src/modules/users/users.router.ts`), idempotent on retry; surfaced and fixed a real gap where most FKs had no `ON DELETE` action at all (`20260801000000_account_deletion_cascades.sql`, see `docs/DATABASE.md`/`docs/ARCHITECTURE.md`); mobile `SettingsScreen` (new — also the app's first sign-out UI and home for the ToS/Privacy links below) uses the same hold-to-confirm interaction as Emergency Exit for this irreversible action
- [x] Real color palette pass across every screen, replacing the neutral-grayscale tokens; update `DESIGN_GUIDELINES.md`'s "intentionally deferred" note — `theme/tokens.ts`'s new `colors` export (§12), a real teal `primary` accent applied to every CTA/active-state/progress element (previously flat neutral, indistinguishable from body text) plus a consolidated text/surface scale and real `danger`/`warning`/`success` semantics; swept across all 17 screens + `StatusBanner` (218 literal replacements), full suite green
- [x] App icon design (all required iOS sizes + Android adaptive icon) — currently just React Native's empty placeholder `AppIcon.appiconset` — placeholder programmer art (a teal `colors.primary` "focus ring" glyph, no real brand design pass yet — flagged in `docs/STORE_LISTING.md`'s pre-submission checklist): all 9 iOS sizes generated (`AppIcon.appiconset`'s `Contents.json` now references real files), Android gets a real adaptive icon (`mipmap-anydpi-v26` background/foreground layers + legacy flat PNGs at every density), verified by an actual `assembleDebug`
- [x] Privacy nutrition label (Apple) / Data Safety form (Google) — now backed by a real data-collected audit (points/rewards, device tokens, attestation `raw_response`, friend graph, username, venue data) and the retention policy above — `docs/STORE_LISTING.md`'s data-type table; actual form submission is owner-actioned (needs each store's live portal)
- [x] App Store Connect / Google Play Console listing content: screenshots, description, keywords, support URL, category, age rating — description/keywords/category/age-rating recommendation drafted in `docs/STORE_LISTING.md`; screenshots need a real running build on a real/simulated device (gated on `docs/MANUAL_QA.md`'s device-availability items) and are NOT included
- [x] Crash/error monitoring (Sentry or equivalent) wired into both `apps/server` and `apps/mobile` — `services/monitoring.ts` in each, ports-and-adapters same posture as attestation/push: a real, tested seam, no-op until a `SENTRY_DSN` exists (`SENTRY_DSN` optional server env var; `config/monitoring-config.ts` on mobile, empty pending real per-environment config). Native Android/iOS build-time plugin wiring (source map/dSYM upload) is NOT done — needs a real Sentry auth token; deferred to the Credentials runbook

**Credentials** *(owner-actioned account/credential creation; the code and runbook side is buildable now, real activation is not)*
- [x] Confirm Apple Developer Program + Google Play Console account enrollment status — prerequisite to nearly everything in the iOS/Android/store tracks above — **confirmed active by the owner, 2026-07-29**; unblocks the iOS/Android/store tracks procedurally, though the actual portal actions (certs, listing submission) still need the owner's login
- [ ] Firebase project + FCM credentials, Apple Push key: obtain, then wire the real `NotificationSender` adapter (replacing `unconfiguredNotificationSender`) and link a real native push SDK (replacing `pushRegistration`'s placeholder)
- [ ] Play Integrity service account + App Attest DeviceCheck key: obtain, then wire real adapters (replacing `unconfiguredAttestationProvider`), review `verdictToTrustTier()`'s denylist against real verdict data, and flip `ATTESTATION_ENFORCEMENT_ENABLED=true`

**Quality gates**
- [x] Detox/Maestro E2E suite across golden paths (create → join → complete; create → emergency exit), run in CI against the new staging deployment (carried over from the original draft) — `apps/mobile/e2e/` (6 Maestro flows + `run.sh` + a Mailpit-OTP-reading helper); join uses a same-device two-account stand-in via "enter code manually" (real cross-device QR scanning stays manual QA). **Written, not executed** — (updated 2026-08-03: an Android emulator now *does* exist on this machine and runs the app, so only the Maestro CLI is still missing; iOS Simulator still needs a Mac — see `e2e/README.md`); running these is now a much shorter step than when this was written
- [x] Load-test the Realtime channel — proposed default target: 500 concurrent connections (modeling a launch-day mix of ~10 simultaneous venue-scale `static_qr` sessions near their 200-participant `VENUE_SESSION_MAX_PARTICIPANTS` cap); confirm/adjust the number before running — `apps/server/loadtest/realtime-load-test.js`, run for real against the local stack: 100% success at 100 concurrent, but 300+ hits a real Supabase Realtime **server-side connection cap** (`ConnectionRateLimitReached`), not a bug — the 500 target can't be confirmed on local/free-tier infra at all; needs a Supabase plan tier whose Realtime limit supports it, or the target revised down (owner decision, see `loadtest/README.md`)
- [ ] TestFlight + Play internal testing track: real beta users on real devices, closing the manual-QA gap hardware constraints have left open across the whole project

**Post-Phase-7 fixes** *(found by running the app, not by the test suite)*

- [x] **First real Android run + the startup crash it caught** (2026-08-03, branch `fix/rn-url-polyfill`) — the app had never actually run anywhere until now. On the first launch it crashed before rendering: `TypeError: Cannot assign to property 'protocol' which has only a getter`, thrown inside `createClient()` from `App`'s first effect (`attachAuthStateListener`). Cause: `@supabase/supabase-js` mutates `url.protocol` to derive its realtime endpoint, but React Native's built-in `URL` has a getter-only `protocol`. Fixed by adding `react-native-url-polyfill` and importing `react-native-url-polyfill/auto` first in `index.js`; guarded by `__tests__/url-polyfill.test.ts` (an on-disk contract test — under Jest the global `URL` is Node's, which *does* allow the assignment, so a runtime assertion would pass with or without the fix and prove nothing). The unit suite could never have caught this: `supabase-client.test.ts` mocks the SDK virtually, so the real constructor never runs — convention now recorded in `.claude/skills/supabase-integration/SKILL.md`. Post-fix the app renders Onboarding 1–3 → Screen 2 with working navigation; see `docs/MANUAL_QA.md`'s Phase 0 entry for what the emulator did and did not verify.

**DoD:** E2E suite green in CI against a real staging deployment (not just local Supabase); production API deployed and reachable with hardened security headers/rate-limiting; account deletion works end-to-end and is reachable from the app; ToS/Privacy Policy content exists and is linked from onboarding; iOS compiles for the first time via cloud CI; Android release build uses the restored multi-ABI + real signing config; both store listings' privacy labels are accurate; a beta ring has run on real hardware before public submission. Entitlement/credential items (Family Controls, FCM/APNs, Play Integrity/App Attest) are approved, or the fallback is explicitly documented — the same "confirmed or documented" bar the original draft's DoD already set.


---

## Phase 8 — Black + Navy Theme & Onboarding Restyle

Owner-approved 2026-08-03, **implemented 2026-08-04**. The plan it was built from is [docs/NAVY_THEME_PLAN.md](docs/NAVY_THEME_PLAN.md); the authoritative description of what shipped is `DESIGN_GUIDELINES.md` §5/§6/§9/§11/§12.

- [x] **Repaint the whole app to a pure-black palette with a navy-blue accent** — replaces the Phase 7 light teal palette outright. Verified as a single-file change: all 17 screens already read semantic tokens from `theme/tokens.ts` with zero raw hex literals, and no spec asserts a colour. Adds `primaryGradientStart`/`End`/`primarySubtle`, `typography.displayLarge` (34), `sizing.heroLogo` (160) and `sizing.iconBadge` (100)
- [x] **Screen 1 (Onboarding)** — collapse the 3-page carousel to a single welcome page: ring-logo hero, `displayLarge` title, muted body, full-width navy gradient CTA. Drops the `howSessionsWork` / `whyPermissionsMatter` copy from both locales (owner-accepted)
- [x] **Screen 2 (Permission Priming)** — 100pt circular badge holding the ring mark, centred copy, gradient CTA. **Plus the one deliberate behaviour change in this phase:** a "maybe later" link during the *priming* state, not only after a denial (owner chose to match the reference; expect a lower screen-time permission grant rate)
- [x] **Screen 3 (Auth)** — same visual language, no behaviour change; the OTP flow and account-linking dialog keep their existing tests unmodified
- [x] **App shell** — mount `SafeAreaProvider` (its first real use) and a light-content `StatusBar`; fix the iOS `LaunchScreen.storyboard` and Android `windowBackground` white cold-start flashes
- [x] **Fallout the plan already identifies** — `App.spec.tsx` presses an `onboarding-skip` that ceases to exist; two Maestro flows tap it under `optional: true` so they no-op and then fail at an unrelated assertion; `AuthScreen`'s dialog uses `colors.background` and would become invisible; the safe-area Jest mock needs `.default` or every spec touching `SafeAreaView` throws
- [x] **Docs** — `DESIGN_GUIDELINES.md` §5, §6, §9, §11, §12 (§12's teal palette is superseded); `MANUAL_QA.md`'s obsolete onboarding-carousel section; `CLAUDE.md` status

**DoD — met.** Mobile suite green at **58 suites / 696 tests** (baseline 55/667; +29 net, all deliberate — see below); lint and typecheck clean; no carousel, dots, skip or `next` copy anywhere, including `App.spec.tsx` and both Maestro flows; `en`/`he` in parity; exactly one behavioural change (the priming-state "maybe later") with every other Screen 2 and Screen 3 test passing **unmodified**. No database changes — `supabase test db` not required.

Worth knowing for a future session:
- **The repaint really was a one-file change.** Changing the values in `theme/tokens.ts` turned all 17 screens dark with the full suite still green — the payoff for the Phase 7 rule that screens hold no hex literals. The only screen that needed a code edit was the Auth account-linking dialog (filled with `background`, invisible over a black scrimmed page), and it now has a test.
- **The palette's contrast ratios are asserted, not just documented.** `tokens.test.ts` computes WCAG relative luminance and fails if any text token drops below 4.5:1 on either `background` or `surface`, or if either CTA gradient end loses its 3:1 edge against the page or its 4.5:1 against the white label.
- **Three new shared components** — `LogoMark` (the ring mark drawn with two Views, at ratios measured off the real 1024px app icon; no icon library and no `react-native-svg` needed), `IconBadge`, and `GradientButton` (RN's built-in `experimental_backgroundImage`, so no native gradient package, no Podfile/Gradle change, and no risk to the macOS iOS build job).
- **An ESLint config gap was found and fixed**: the test override globbed `**/*.test.ts` but not `**/*.test.tsx`, so component tests were being held to the no-literal-string rule meant for production code.
- **Not verified here, needs a device or an SDK:** the Android window-background change is pinned by an on-disk contract test but **not build-verified** — this container has no Android SDK and CI does not build Android. Same for the gradient actually painting, the launch surfaces not flashing white, and a real look-through of all 17 repainted screens. All are in `docs/MANUAL_QA.md`.
- **Follow-up pass (same day), after auditing the repaint rather than trusting its green suite** — three real defects and one structural gap:
  1. **A WCAG failure the repaint introduced.** Auth's Terms/Privacy links used `colors.primary` as *text* — 3.93:1 on black, under the 4.5:1 bar. Fixed with a new `link` token (`#5B8AF0`, 6.34:1). More importantly the *hole* is closed: `tokens.test.ts` checked `primary` against the 3:1 non-text bar (correct for a button) and text tokens against 4.5:1, but nothing connected the two, so a fill-only colour could become text with the suite green. `text-color-usage.test.ts` now reads every screen/component source and fails on any `color: colors.X` where X is not text-legible.
  2. **Auth rendered inline errors in `textPrimary`** while eight other screens use `danger` — on a dark theme the wording became the only signal of failure. Now `danger`.
  3. **The suite was not deterministic, and had not been before this branch.** The pre-theme baseline failed ~7 runs in 12, always in the permission-flow tests. Cause: all **32** `jest.mock(...)` calls carried `{ virtual: true }` while targeting modules that actually exist — flags written in Phase 0/1 before those packages were installed, never removed once they landed. A virtual mock of a resolvable module leaves jest's mock-vs-real choice non-deterministic across workers, so `blockingPermissions.request()` intermittently hit the real module. The repo had already diagnosed this exact failure once (see `auth-service.test.ts`'s comment) and fixed only the instance in front of it. Now 0 failures in 15 consecutive full-suite runs.
  4. **CI never compiled Android** (`server`, `mobile`, `db`, `ios-build` only), which is *why* the theme's native edits shipped unverified — Jest can assert a resource file contains a string, never that the project builds. Added an `android-build` job running `assembleDebug` (debug keystore, no secrets; single ABI, since "does it compile and link" is ABI-independent). A local build was attempted first and is impossible here: the network policy denies `dl.google.com`, so no Android SDK can be installed in this container.

     The job earned its place immediately: its **first run failed** with `./gradlew: Permission denied`. Not a job bug — `gradlew` had been committed mode `100644` since the project was scaffolded, so `./gradlew` had **never** worked from a fresh clone on Linux or macOS, and `e2e/run.sh` (whose README says to run `./run.sh android`) had the same problem. Both are now `100755`. With that fixed, `assembleDebug` compiled green in ~7 minutes — the theme's Android resource changes are build-verified for real, not just asserted on disk.

- **Open follow-up:** the launcher icon is still teal `#0F6B5C` on a now black-and-navy app (owner chose to keep the logo as-is), and the two dropped onboarding pages' copy ("how sessions work", "why permissions matter") is now explained nowhere — both are product calls, not engineering ones.

---

## Phase 9 — Host-selected Blocklist

Owner-approved 2026-08-07. The plan is [docs/BLOCKLIST_SELECTION_PLAN.md](docs/BLOCKLIST_SELECTION_PLAN.md) — read it before touching any task here; every design question it raised is already decided in it.

Reverses ARCHITECTURE §4's "fixed set of default categories… not a per-session or per-user app picker": the host names *what* to block on the Create Session screen, and each member's device resolves that name against its own installed apps. Two kinds of name, both plain strings so they can travel between phones: a **category** (`social` — every app on *that* device declaring itself social) and an **app** (`com.instagram.android` — that specific app, on devices that have it). Six categories, up from three.

Dependency order: 1 and 2 are independent roots; 3 depends on 2, 4 and 5 on 3, 6 on 1, 7 on 1 and 5, 8 on 1.

- [x] **Schema + server** — migration with backfill (`sessions` *and* `venues`), the six categories, pgTAP for the constraints and the table-wide grant, zod validation on create, the safety denylist, the venue subset check, preview fields — `20260807000000_host_selected_blocklist.sql` (both tables, backfill before constraint, `chk_blocked_categories_valid` / `chk_blocklist_non_empty` / `chk_venue_approved_categories_valid`); `apps/server/src/modules/sessions/blocklist.ts` holds the six-category vocabulary, the package-name pattern, `SAFETY_DENYLIST` and the two pure checks. `POST /sessions` gains `blocked_categories`/`blocked_packages` (defaulting to the historical three, so a pre-Phase-9 client is unaffected), 400 `blocked_package_not_allowed` for a denylisted app, 403 `blocklist_not_venue_approved` for a `static_qr` session outside its venue's approved set; `POST /sessions/preview` returns both. 250/250 pgTAP (23 new), 324/324 server unit tests, 29/29 integration (3 new — the only tests that prove the arrays survive a real PostgREST round trip, since every unit test runs against a fake store), lint + typecheck clean. Docs: `DATABASE.md` schema + config table (the `BLOCKED_APP_CATEGORIES` row is gone), `ARCHITECTURE.md` §4 rewritten and §10 extended. **Two judgement calls the plan left implicit:** the venue subset check applies to `static_qr` only (a host running their own `dynamic_qr` session at their own venue is choosing for people who chose to join *them*), and the denylist compares case-insensitively.
- [x] **Catalog research** *(research task, owner-requested)* — produce the catalog data itself: most-used and currently-trending apps worth naming, weighted toward social per §6, each with Android package name, category, and iOS URL scheme where one exists. Also picks the ≤50 schemes to declare. Deliverable is the reviewed data file, nothing else — `apps/mobile/src/config/app-catalog.json`, 87 entries (33 social / 18 entertainment / 16 games / 9 productivity / 7 news / 4 maps), 40 iOS schemes so there are 10 spare slots under Apple's 50 cap. Reasoning, selection rule and refresh instructions in the new `docs/APP_CATALOG.md`. **No code** — task 3 builds the module around it. **Two things worth knowing:** a *wrong* `iosScheme` is worse than a missing one (a scheme that doesn't resolve hides the entry from a host who genuinely has the app), so 47 entries deliberately carry none rather than a guess — and TikTok's is `snssdk1233`, not `tiktok`, which is exactly the kind of thing writing it from memory would have got wrong. Nothing here is verifiable on this machine: every package name and every scheme needs a real listing/device check, now in `docs/MANUAL_QA.md`, along with the one ambiguity already found (HBO Max has two live Play listings, `com.wbd.stream` and `com.wbd.hbomax`)
- [x] **Catalog module + source seam** — lookup, the seam the picker reads through, and an integrity test (unique ids, valid categories, well-formed package names, scheme count ≤ 50). Includes the iOS `canOpenURL` installed-check behind the same seam (mocked in tests; real probing is manual QA) — `config/app-catalog.ts` (boundary-validates the JSON, derives `IOS_APPLICATION_QUERIES_SCHEMES` from it so the two can't drift), `config/blocklist-safety.ts` (the client half of the denylist — a second copy of the server's by necessity, since the server can't ship code to the client; each side pins its own), `services/blockable-app-source.ts` (the seam). **The category list went three → six here, not in task 6**, because nothing about the catalog can be typed until it does; `app-blocker.ts`'s duplicate `BLOCKED_CATEGORY_VALUES` array is gone, replaced by `isBlockedCategory` from the one declaration — that duplicate is exactly what would have silently dropped every `shield_triggered` event for the three new categories. Kotlin's `CategoryMapping.kt` still has its own copy and stays task 6's job. **`installed` is three-valued, not a boolean** (`installed`/`not_installed`/`unknown`): iOS can only probe the ~50 declared schemes, so for everything else its silence proves nothing, and the §7 "you don't have 2 of these" note needs to tell "known absent" from "can't tell". Android deliberately never probes `canOpenURL` — it means something different there (the manifest intent-query allowlist, not presence), so it would give confidently wrong answers rather than none. The 40 schemes are now in `ios/LockalTime/Info.plist`, pinned against the catalog by `__tests__/native-config.test.ts` — drift there fails in the worst direction (an undeclared scheme hides an app the host actually has, and nothing about it looks like an error). Mobile suite 737/737 (62 suites, +33), lint + typecheck clean. **Pre-existing flake found, not caused by this task:** on a *cold* jest cache the first test in four screen specs (`HomeScreen`, `HistoryScreen`, `SessionDetailsScreen`, `SessionCompletionScreen`) times out at the default 5s while babel transforms the RN + i18n graph inside the first `render`. Reproduced identically on unmodified `main` (4 failed / 704 there too), green on every warm run. CI checks out fresh, so CI is running with a cold cache every time — worth a `testTimeout` bump or a warm-up, tracked as a follow-up below
- [x] **`InstalledAppsModule`** — Android native module behind that seam, windowed icon call, JS-side contract test over a mocked bridge — `InstalledAppsModule.kt` (registered via the existing `AppBlockerPackage`, no second `ReactPackage`) + `services/installed-apps.ts` + the seam's Android branch. **Kotlin actually compiles here** (`./gradlew :app:compileDebugKotlin`, green) — this machine does have an Android SDK, so the native side is build-verified, not just asserted on disk. Three things worth knowing: the enumeration is LAUNCHER-intent filtered, which is doing real work rather than tidying (a raw `getInstalledApplications()` returns hundreds of headless system services nobody would recognise in a picker); the default dialer and SMS app are excluded via `TelecomManager`/`Telephony.Sms`, the only *device-accurate* way to name them, with the JS and server static denylists as separate layers rather than substitutes; and it runs on a plain single-thread `Executor` rather than adding a kotlinx-coroutines dependency for two call sites. `QUERY_ALL_PACKAGES` is declared in the manifest with the refusal path spelled out — **the mitigation is exercised by a test, not just described**: `blockableAppSource` falls back to the catalog whenever enumeration returns empty, and empty is a safe trigger because it is never a truthful answer (a device running this app has at least this app). Mobile suite 760/760 (63 suites, +23), lint + typecheck clean. Docs: `DEPLOYMENT.md` gains the Play declaration runbook and the iOS scheme-list review note, `MANUAL_QA.md` the six real-device checks, `ARCHITECTURE.md` §4 the module
- [x] **Create Session UI** — six category toggles, app picker, validation, persisted default — new `components/BlocklistPicker.tsx` (six toggles + a virtualized app `FlatList`, `accessibilityRole="checkbox"` + `accessibilityState` on every row) and `state/blocklist-preference-store.ts` (persisted last choice, `active-session-store`'s hydrating/ready gate and fail-open policy; hydrated in the App bootstrap but deliberately **not** part of the render gate — it only pre-fills a field on a screen nobody can reach during cold start). Wired into `CreateSessionScreen` with the non-empty guard, venue narrowing, and the two new server refusals (`blocklist_not_venue_approved`, `blocked_package_not_allowed`) mapped to their own copy. 30 new en/he keys. **A real bug the tests caught:** each app row's `onPress` closes over the *whole* selection, and `rows` doesn't change when only a category does — so without `extraData={selection}` the cells kept a stale closure and a category picked just before tapping an app was silently dropped. **Two deliberate design calls:** `installed` stays three-valued through to the UI, so the "not on this device" note counts only *known* absences and never iOS's "couldn't tell" (reporting that as absence would tell the host something false about their own phone); and a selection carried over from a previous session that the source no longer offers still gets a visible, de-selectable row — an invisible selection that is nonetheless submitted is the worse outcome. Mobile suite 805/805 (65 suites, +45), green on three consecutive runs, lint + typecheck clean. **Test-harness note worth keeping:** `BlocklistPicker`'s `FlatList` schedules cell work on a timer that holds an `act()` scope open, so a bare `fireEvent.press` can be observed before the render it caused — `CreateSessionScreen.spec.tsx` now settles on committed `accessibilityState` between interactions rather than trusting press ordering
- [x] **Enforcement wiring** — packages and the three new categories through `start()`, service poll, `CategoryMapping`, `BootPersistence`, `SessionRow`/`SESSION_COLUMNS`, the widened `shield_triggered` shape, and overlay copy that names the blocked app — all of it, Kotlin build-verified (`:app:compileDebugKotlin` green). **A specific app wins over its category**, and not just as an ordering: it decides what the event says, and "Instagram is blocked" beats "social is blocked" whenever the host actually named that app. `shield_triggered` is now discriminated on `reason` (`'category' | 'package'`) rather than a nullable category — a package-triggered block had no valid value for the old field and was being **silently dropped by the boundary validation**, which looks exactly like no event at all. **The overlay copy moved into JS**: it was one generic Android string resource with no i18n path whatsoever (a bare Kotlin `TextView`, outside i18next, no `values-iw`), so the already-translated text is handed across the bridge at `start()` and the service substitutes the app's own `PackageManager` label — one translation source of truth instead of a second one to keep in sync. `BootPersistence` carries the packages and that copy too, because a reboot restarts the service with JS never running; omitting them would resume with a partial blocklist, silently under-blocking exactly the apps the host singled out. **One real fix beyond the plan's list:** `useAppBlocker` now stabilizes the blocklist arrays *by content*. They come off the hydrated session row, which is a fresh object on every realtime update, so no caller could hand over a stable reference — the old "pass a stable reference" contract was a trap with no compiler behind it, and honouring it naively would have torn down and restarted the blocker (lifting the shield for a moment) every time anything about the session changed. Mobile suite 825/825 (+20), lint + typecheck clean
- [x] **Join flow** — Screen 8 blocklist display, iOS re-selection in Apple's picker with header text, the blocklist-keyed selection cache, and the token-learning map — with its decision rule as a unit-tested JS pure function over `getKnownIds()`, not Swift. Both platforms — `services/blocklist-display.ts` (shared with task 8), `services/ios-blocklist-selection.ts` (the pure rule + cache key), `services/ios-family-controls.ts` (the seam and the orchestration), and three new Swift files: `BlocklistTokenStore` (App Group keyed store), `IosFamilyControlsModule`, `BlocklistPickerHostView`. **The Swift/JS split is the point of the whole task**, and it held: Swift stores and applies, JS decides, and every branch of the decision rule has a unit test — including the one that matters most, that two unknowns learn *nothing* rather than guessing which token is which (a wrong map entry would be permanent and undetectable). The picker runs **before** the join call, not after: a join that succeeded followed by a dismissed picker is the half-joined state §9 forbids — present, enforcing nothing, still earning — and a wasted picker before a full session is the better of the two failures. Screen 8's list de-duplicates category/app overlap (§9): pick `social` *and* Instagram and only "Social" is shown, since listing both makes the session look like it blocks more than it does. Mobile suite 872/872 (68 suites, +47), lint + typecheck clean, Kotlin still compiles. **Everything iOS here is unverified** — the eight-item checklist in `docs/MANUAL_QA.md` covers what the split could not, in particular the learn-by-subtraction round trip, which is unobservable from this machine. New Swift files need no `wire-blocking-target.rb` change: it globs `LockalTime/Blocking/*.{swift,m}`
- [x] **In-session display** — Screen 6's expandable blocklist, read-only, off the already-hydrated session row — `components/BlocklistSummary.tsx`, collapsed to two entries plus a `+N` chip, expanding on tap, `accessibilityState.expanded` for screen readers. Read-only *by construction*: no callback, no edit affordance, so the §9a freeze cannot be violated by this surface even accidentally. Reuses `describeBlocklist`, so Screen 6 and Screen 8 word the same blocklist identically and de-duplicate it the same way. Mobile suite 881/881 (69 suites, +9). **A second test-harness lesson, same family as task 5's:** `render()` must be `await`ed and so must `fireEvent.press` — without it RNTL's `screen` is still the not-implemented default when the assertions run, and a press is observed before the render it caused. Every other spec in this repo already awaited both; this is now understood rather than copied

**Phase 9 follow-up — platform parity** *(owner decision 2026-08-08, after the phase closed)*
- [x] **Both platforms offer the same fixed app catalog** — reverses the 2026-08-07 "full enumeration on Android" choice. Android no longer enumerates the device; it filters the same 87-entry catalog iOS uses, via a manifest `<queries>` block generated from the catalog, and answers one narrow per-app question exactly as iOS does with `canOpenURL`. **`QUERY_ALL_PACKAGES` is gone**, and with it the Play Console declaration, its weeks of review and the risk of an outright refusal blocking release — once the catalog is the only way to name an app anywhere, enumeration bought nothing but that risk. `InstalledAppsModule.getInstalledApps()` became `getInstalledPackages(names)`; `installedAppsSource`/`catalogAppSource`/`isExhaustive` collapsed into one source. **A real bug the new tests caught:** the first cut returned an empty set on bridge failure, which made every app read as *not installed* — a false claim about the host's own phone that would have hidden apps they actually have. `getInstalledPackages` now returns `null` for "could not ask" as distinct from an empty set for "you have none". Two regression guards in `__tests__/native-config.test.ts`: the `<queries>` block must match the catalog exactly (an undeclared package reports absent whether it is there or not, with nothing resembling an error), and `QUERY_ALL_PACKAGES` must not reappear. Mobile 883/883, lint + typecheck clean, Kotlin compiled and the merged manifest verified to carry all 87 entries. **Owner also decided:** keep the catalog at 87 for now rather than expanding it, and revisit once there is signal about what people actually pick. Honest cost, now in `docs/APP_CATALOG.md`: an app outside the catalog cannot be named by anyone, on any platform — categories still cover the long tail

- [x] **Catalog: the HBO Max ambiguity resolved, by listing both.** Both Play listings are live and separately maintained — `com.wbd.hbomax` ("HBO Max") and `com.wbd.stream` ("Max"), left over from the Max rebrand and the revert — so picking one would have silently failed for everyone with the other. Both are now catalog entries under their real store names, which works precisely *because* the picker filters per device: a host only ever sees the one they have, so two rows never read as a duplicate. No alias machinery needed. Catalog is 88 entries; the `<queries>` block regenerated and the merged manifest re-verified. **TikTok's regional split is deliberately NOT handled this way** and stays open in `docs/MANUAL_QA.md`: `com.zhiliaoapp.musically` vs `com.ss.android.ugc.trill` is the same app under the same name, so two rows would just look like a bug — an optional `aliases` field is the shape to reach for if the en/he target markets ever need it
- [x] **iOS 16 API caught before CI did** — `BlocklistPickerHostView` used `FamilyActivityPicker(headerText:footerText:selection:)`, which is **iOS 16+** against this project's 15.1 deployment target: a compile error, not a runtime degradation, and one only cloud macOS CI could have surfaced. Replaced with our own SwiftUI header/footer around the plain picker — one code path rather than an `if #available` branch, since Swift is the one place here that cannot be tested and a second untested branch costs more than it buys. The cosmetic loss (our chrome sits outside Apple's list rather than inside it) is now a `docs/MANUAL_QA.md` check: if the instruction scrolls away when the member taps Apple's search field, an availability-gated native header is the fix. Swift 5.7 shorthand optional bindings also replaced with the explicit form — nothing pins the Xcode version CI uses. **This is exactly what `platform-constraints`'s "check availability before using a Screen Time / SwiftUI API" rule exists for, and it was missed on the first pass**

**Phase 9 — still open** *(nothing code-side; these need hardware, a CI run, or the owner)*
- [ ] **Confirm CI is green on Phase 9 — the `ios-build` job especially.** Everything here was verified locally, but **no CI run has been observed for any of it**, and the macOS `ios-build` job is the *only* thing that compiles the five new/changed Swift files. One iOS 16 API (`FamilyActivityPicker(headerText:footerText:)` against a 15.1 target) was already caught by inspection after being written; that is the class of defect only a real compile will rule out. Check the runs for commits `d59eed2`..`d43caff`
- [ ] **Push the Phase 9 migration to staging, then production** — `20260807000000_host_selected_blocklist.sql`, verified against local Supabase only. Manual by convention (`CLAUDE.md`); staging first (`docs/DEPLOYMENT.md`)
- [ ] **Work `docs/MANUAL_QA.md`'s Phase 9 checklist** — 33 items across six sections, none runnable here. The two that most deserve attention: the **learn-by-subtraction round trip** on a real iPhone (the token-learning feature is entirely unobservable from this machine), and **every catalog package name against its real Play listing** (a wrong one blocks nothing, forever, with no error anywhere)

**Found during Phase 9, not part of it** *(closed)*
- [x] **The mobile suite failed 4 screen specs on a cold jest cache.** `HomeScreen`, `HistoryScreen`, `SessionDetailsScreen`, `SessionCompletionScreen` each timed out at the default 5s on their *first* test, because the babel transform of the whole RN + i18n dependency graph happens inside that test's `render` — so one test pays for the entire module tree. Reproduced on unmodified `main`, so it predated Phase 9; the part that mattered is that **CI checks out fresh every run**, meaning CI had been running cold all along and passing on margin rather than by design. Fixed with `testTimeout: 20000` in `jest.config.js` — the right lever, since nothing here is genuinely slow and a real hang still fails, just 15s later. Verified by `npx jest --clearCache` twice: 883/883 both times (~32s cold, ~16s warm), where it previously failed 4 every time

**DoD:** a host can pick categories and specific apps at creation; the choice reaches every member's device and is enforced there; a `static_qr` session cannot exceed its venue's approved set; the safety denylist is refused server-side, not merely hidden in the picker; the blocklist is frozen for the session's lifetime; suites green in every workspace touched, plus pgTAP. iOS behaviour, `canOpenURL` probing, icon performance, the Play `QUERY_ALL_PACKAGES` declaration outcome and iOS token rotation are all unverifiable here and belong in `docs/MANUAL_QA.md` (plan §12).
